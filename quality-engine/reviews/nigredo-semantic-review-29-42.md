# Nigredo semantic review — manifest posts[28:42]

**Gate reviewed:** Fool's Wisdom Grounding Gate v2.2.0 (complete)
**Scope:** fourteen source posts and their current Nigredo audits; semantic correctness only
**Method:** full source-to-inventory sweep, atomic-claim splitting, history/analogy routing, taxonomy, strict removal-test load, duplicated-load check, and recovered Nigredo thresholds kept separate from later Rubedo routing

The counts below are corrected **minimums** after the listed mandatory changes. Where a row must be split, the new total includes every specified child row. “Load at most” is deliberate: it removes only demonstrably duplicated `Y` flags and does not pretend that unlisted editorial judgment is arithmetic.

## Verdict summary

| # | Post | Verdict |
|---:|---|:---:|
| 1 | `seventeen-ways-pattern-repeats.md` | FAIL |
| 2 | `shadbala-tensor-field-theory.md` | FAIL |
| 3 | `spolski-endocrine-framework.md` | FAIL |
| 4 | `sri-yantra-geometry-that-doesnt-fit.md` | FAIL |
| 5 | `the-devil-in-the-detail.md` | FAIL |
| 6 | `the-downstream-mind.md` | FAIL |
| 7 | `the-ineffable-secrets-of-a-breathing-sprite.md` | FAIL |
| 8 | `the-source-code-has-authors.md` | FAIL |
| 9 | `the-sun-names-you.md` | FAIL |
| 10 | `the-tower-speaks-in-richter-scale.md` | FAIL |
| 11 | `vimshottari-dasha-markov-chains.md` | FAIL |
| 12 | `vortex-based-mathematics.md` | FAIL |
| 13 | `water-fourth-phase.md` | FAIL |
| 14 | `your-reality-is-a-smart-contract.md` | FAIL |

## 1. `seventeen-ways-pattern-repeats.md` — **FAIL**

1. **Accurate history is misrouted as borrowed authority.** Source L64, “it is now standard crystallography”; current audit row L64 is `science / AUTHORITY-BORROWED / N`. Recode to `ALIGNED / N`: it reports the accurate status/history of the classification and is not the warrant for the separately inventoried theorem. Source L113, “Coxeter remains the standard modern reference”; current row L113 is likewise `AUTHORITY-BORROWED / N`. Recode to `ALIGNED / N`. Add separate `ALIGNED / N` history rows for L113, “Schattschneider's 1978 paper…” and “Conway, Burgiel, and Goodman-Strauss… developed the modern orbifold notation”; one sentence contains three independent historical claims.
2. **The universal at L66 is hidden by a narrowed audit note.** Source L66 says “Every yantra, every textile, every Escher tessellation…”; current math row L66 quotes only “falls into one of these seventeen classes” and calls it `INTEGRATED / Y` by silently adding “genuinely periodic.” Split it: retain `INTEGRATED / Y` only for “every periodic ornament in 2D,” and add `WRONG / N` for the unqualified yantra/textile/Escher universal because finite or non-periodic figures need not have wallpaper-group symmetry.
3. **Correct 8-fold restriction content is omitted.** Source L125, “Pure 8-fold rotation is not a wallpaper group. The crystallographic restriction excludes it”; no current row. Add one `math / INTEGRATED / N` row. It is correct but repeats the already load-bearing restriction theorem.
4. **House grammar is being allowed to launder false mathematics.** Source L153, “Kha witnesses which class the cadence selects”; current row L153 is `math / INTEGRATED / Y`. Recode to `WRONG / N`: Kha-Ba-La is canonical, but the underlying cadence-to-wallpaper-class claim still lacks a planar motif, two translations, and a group action; it also repeats the L68 thesis.
5. **Duplicated conclusions are over-coded `Y`.** Current failing math rows L151, “The traditions found the high-valence subset,” and L153 repeat the already load-bearing L68 claim. Change both to `N`; removing either closing repetition does not collapse the post.

**Corrected minimum:** science 18 (`ALIGNED 5, GROUNDED-OBSERVATIONAL 0, AUTHORITY-BORROWED 3, CONTESTED-AS-FACT 9, FABRICATED 1, INVERTED 0`); math 31 (`INTEGRATED 12, DECORATIVE 1, WRONG 18`); dross 32; failing load at most 23; **Nigredo remains MAJOR DROSS**.

## 2. `shadbala-tensor-field-theory.md` — **FAIL**

1. **The traditional Shadbala inventory is omitted.** Source L39–L44 gives six named Jyotish bala definitions; the current audit includes only the erroneous Naisargika ordering as math. Add six `science / ALIGNED / N` rows, one per bala. The traditional house map belongs in the science inventory even though the later tensor conversion fails.
2. **The tensor definition is incompletely and incorrectly accepted.** Source L59, “Each component is independent”; current audit only inventories “independence is the defining feature of a tensor.” Add `math / WRONG / Y`: no independence is demonstrated. Source L63, “generalizes scalars … vectors … and matrices … to arbitrary dimensions”; current row is `INTEGRATED / Y`. Recode to `WRONG / Y`: a matrix is not a tensor absent a transformation law, and tensor rank is not “arbitrary dimensions.”
3. **L84 is not atomically split.** Current rows cover the 3×3×3 placement and stress-energy analogy, but omit “diagonal elements carry the primary strengths” and “off-diagonal elements carry the interaction terms.” Add two `WRONG / N` rows; six inputs cannot populate those independently asserted slots, and the claims repeat the already load-bearing invalid mapping.
4. **L86 bundles three claims into one row.** Keep the current contraction row `WRONG / Y`; add `DECORATIVE / N` for “the trace loses information” (true general background but removable from this invalid construction) and `WRONG / N` for “The full tensor preserves the directional character,” because no valid tensor or direction has been defined.
5. **The displayed program does not implement its own claim.** Source L99 calls nonexistent `ShadbalaSystem.calculate_tensor_field`; no current row. Add `math / WRONG / N` for the absent construction.
6. **Four gradient claims are collapsed into one.** Current L110 row covers only “reveals where planetary influence is increasing.” Add separate `WRONG / N` rows for “positive gradient is gaining influence,” “negative gradient is losing it,” and “gradient direction tells you which strength component”; the alleged field has no domain or derivative coordinates.
7. **Accurate background and false application need splitting.** Add `DECORATIVE / N` for L116, “determines distances, angles, and curvature”; add `DECORATIVE / N` for L124’s frame-dependent electric/magnetic decomposition. Both are accurate background but removable from Shadbala. Add `WRONG / N` for L120, “off-diagonal tensor components … describe how different strength dimensions couple”; `WRONG / N` for L126, “analogous to the speed of light”; and `WRONG / N` for L128, “projected onto lower-dimensional interpretive frameworks.”
8. **The field/coherence claims are under-extracted.** Add `WRONG / Y` for L132, “total Shadbala … defines the field coherence”; add `DECORATIVE / N` for L134, “boundary between order and chaos”; the phrase supplies no Shadbala dynamical system.
9. **The seer-history fabrication is under-split.** Current L136 has `FABRICATED` rows for hand-computed tensor components, tracked gradients, and Einstein-identical mathematics. Add `FABRICATED / N` for “were field theorists working without the formalism” and `FABRICATED / N` for “identified the symmetries and invariants through contemplative practice.” These are independent invented historical claims. Nigredo was already MAJOR; the resulting ≥2 fabrication route still belongs only to later Rubedo `ESCALATE-TO-MANUAL`.
10. **Body mechanisms are omitted.** Add `science / CONTESTED-AS-FACT / Y` for L142, “components … manifest as physiological states”; add `CONTESTED-AS-FACT / N` for the transit causing chest pressure/restlessness/attention changes; add the omitted Saturn-specific L118 mechanism as `CONTESTED-AS-FACT / N`.
11. **A valid vector statement and a non-reference analogy are mishandled.** Add `math / INTEGRATED / N` for L144, “It is a vector with direction,” which repeats the valid six-vector representation. Delete current math row L146, “The natal chart is a kernel. The transit is a process”: these are software metaphors with no mathematical structure, not `DECORATIVE` math.
12. **Repeated terminal claims cannot all be load-bearing.** Change current L144 failing rows “tensor's nonlinearity,” “off-diagonal … coupling terms,” and “Because it is a tensor” from `Y` to `N`; removing these restatements leaves the earlier tensor thesis intact.

**Corrected minimum:** science 30 (`ALIGNED 10, GROUNDED-OBSERVATIONAL 1, AUTHORITY-BORROWED 0, CONTESTED-AS-FACT 13, FABRICATED 6, INVERTED 0`); math 53 (`INTEGRATED 3, DECORATIVE 12, WRONG 38`); dross 69; failing load must be recomputed downward after the specified duplicate `Y→N` changes; **Nigredo remains MAJOR DROSS**. Later Rubedo still routes `ESCALATE-TO-MANUAL` because fabrication count remains ≥2.

## 3. `spolski-endocrine-framework.md` — **FAIL**

1. **The thesis sentence is not fully split.** Source L33, “mystical systems were … biological documentation written in symbolic notation”; no current row. Add `science / CONTESTED-AS-FACT / Y`. The current “correspondences … precise” row does not cover the independent historical/biological-documentation claim. Add L37, “biological and symbolic converge,” as `CONTESTED-AS-FACT / N` because it repeats that thesis.
2. **The first pairing is largely absent.** Add L43, “Type 1 Reformer lives inside this correspondence,” as `CONTESTED-AS-FACT / Y`; add “wrath … when timing is violated” as `CONTESTED-AS-FACT / N`; add L45, “Same function, different substrate,” as `CONTESTED-AS-FACT / N`.
3. **Several pair mechanisms are omitted.** Add L49, “through repeated chemical reinforcement,” as `INVERTED / N`; L51, “Societies … fragment in exactly the same way,” as `CONTESTED-AS-FACT / N`; L57, “what the endorphins mask,” as `INVERTED / N`; L71, “scarcity that stress has taught the body to expect,” as `CONTESTED-AS-FACT / N`; and L95, “Type 9's sloth … Uranian over-identification,” as `CONTESTED-AS-FACT / N`.
4. **Mixed biological clauses require atomic splitting.** Current L81 row treats “Testosterone drives growth, development, and competitive intensity” wholly as `INVERTED`. Split it into `GROUNDED-OBSERVATIONAL / N` for growth/development and `INVERTED / N` for competitive intensity. Current L93 “Serotonin regulates mood and systemic balance” similarly needs two rows: retain `INVERTED / N` for mood; add `CONTESTED-AS-FACT / N` for the undefined systemic-balance claim. Add L69 “focuses the system on survival” as `GROUNDED-OBSERVATIONAL / N`.
5. **Load is multiplied within each repeated pairing.** The strict removal test permits at most one failing identity/mechanism row marked `Y` per hormone–Muse pairing. Change additional same-pair `Y` rows to `N` (melatonin L43/L45, endorphin L55/L57, dopamine L61/L63, cortisol L67/L69/L71, estrogen L75/L77, testosterone L81/L83, adrenaline L87/L89, serotonin L93/L95). The post may depend on each pairing, but it does not collapse when a second paraphrase of that pairing is removed.

**Corrected minimum:** science 57 (`ALIGNED 0, GROUNDED-OBSERVATIONAL 7, AUTHORITY-BORROWED 0, CONTESTED-AS-FACT 35, FABRICATED 1, INVERTED 14`); math 1 (`INTEGRATED 0, DECORATIVE 1, WRONG 0`); dross 51; failing load at most 15; **Nigredo remains MAJOR DROSS**.

## 4. `sri-yantra-geometry-that-doesnt-fit.md` — **FAIL**

1. **Accurate bibliographic history is misrouted or omitted.** Source L102, “Kulaichev's 1984 paper … derives the precise coordinates”; current row is `AUTHORITY-BORROWED / N`. Recode to `ALIGNED / N`: it accurately reports what the paper does and is not itself proof of ritual efficacy. Add L102, “Bühnemann's … compiles many … Indian examples,” as `ALIGNED / N`. Add L138, “there are 230 space groups,” as `science / ALIGNED / N`; the later claim about extra rotational orders remains separately `math / WRONG`.
2. **Correct but removable counts are wrongly `INTEGRATED / Y`.** Recode current L72 “43 smaller triangular regions,” L74 “8-petal lotus,” and L120 “Row n contains the binomial coefficients” from `INTEGRATED / Y` to `DECORATIVE / N`. Removing any of these leaves the D9/planar-mismatch thesis intact; correctness is not sufficient under §4.4.
3. **Outer-rosette claims are omitted.** Add L80 “8-petal lotus is 8-fold” and “16-petal lotus is 16-fold” as two `DECORATIVE / N` rows: finite rosette symmetries are plausible, but neither is needed for the square-frame comparison and neither makes a periodic tiling.
4. **D9 consequences are under-split.** Add L82 “Eighteen total isometries” as `WRONG / N` because the D9 premise is false. Add L84 “No wallpaper group carries 9-fold rotational symmetry / no periodic tiling…” as one `INTEGRATED / N` row; it is correct but duplicates the existing crystallographic-restriction row.
5. **Several false planar/space-group deductions are missing.** Add `WRONG / N` rows for L86 “outer frame carries the highest crystallographic symmetry available (4-fold, 8-fold)” and “non-periodic core inside a periodic outer frame”; L88 “inner core is exactly the symmetry order the plane refuses” and “outer frame is the highest symmetry the plane permits”; and L96 “natural local symmetries include 9-fold.” Finite rosettes are not periodic crystals, 8-fold is not crystallographic, 6-fold is omitted, and standard Sri Yantra is not D9.
6. **A repeated experience claim is omitted.** Add L160, “The geometry was real — discoverable empirically through inhabitation,” as `science / CONTESTED-AS-FACT / N`; it restates the unestablished cross-practitioner mechanism.
7. **Duplicated D9 deductions are over-coded `Y`.** Keep `Y` on the foundational false D9 claim, but change later conclusion rows L90, L138, L140, and L142 to `N` where they merely restate that false premise as curvature/3D/space-group resolution. Removal of any one later restatement leaves the thesis intact.

**Corrected minimum:** science 30 (`ALIGNED 8, GROUNDED-OBSERVATIONAL 1, AUTHORITY-BORROWED 2, CONTESTED-AS-FACT 11, FABRICATED 7, INVERTED 1`); math 41 (`INTEGRATED 5, DECORATIVE 13, WRONG 23`); dross 57; load must be recomputed downward after the specified duplicate `Y→N` changes; **Nigredo remains MAJOR DROSS**. The seven fabrication rows preserve the separate later Rubedo `ESCALATE-TO-MANUAL` route.

## 5. `the-devil-in-the-detail.md` — **FAIL**

1. **Experiment history and experiment-derived mechanism are conflated.** Source L44, “Wolfram Schultz … watched a monkey watch a light”; current row is `AUTHORITY-BORROWED / Y`. Recode this historical event to `ALIGNED / N`; retain the separate neural-result rows as `AUTHORITY-BORROWED`. The Cambridge/neuroimaging mislocation correctly remains `FABRICATED`.
2. **The matter-first thesis sentence is omitted.** Source L52, “The reward prediction error (RPE) signal is the substrate”; no current row. Add `science / INVERTED / Y`.
3. **The conditioning sequence is not atomically inventoried.** Add L54 “B-flat chime was the conditioned stimulus” and “unlock swipe was the response” as two `GROUNDED-OBSERVATIONAL / N` rows. The separately inventoried “unconditioned stimulus” mislabel remains `CONTESTED-AS-FACT`.
4. **Embodied evidence is under-extracted.** Add L64 “phone became an extension of my proprioceptive field” and “compulsion … was an identity layer” as two `GROUNDED-OBSERVATIONAL / N` rows. Split L72 so the intervention and “The loop broke” are separately `GROUNDED-OBSERVATIONAL`; the current row's quote names only the intervention while its note silently counts the outcome.
5. **Pratyāhāra is definitionally misattributed.** Source L74 says it is “not sense withdrawal, but sense specificity”; current row is `ALIGNED / N`. Recode to `FABRICATED / N`: classical pratyāhāra is sense withdrawal/control; directing attention to one point is closer to dhāraṇā. House-tradition protection does not protect inaccurate history/definition.
6. **“Urge surfing” bundles history with an unsupported mechanism.** Current L76 row `AUTHORITY-BORROWED / N` should split: `ALIGNED / N` for the real named clinical technique, and `CONTESTED-AS-FACT / N` for “works because it isolates the detail.”
7. **A self-model mechanism is omitted.** Add L62, “chains have become part of their self-model,” as `CONTESTED-AS-FACT / N`.
8. **Mechanistic purpose is taxonomized too softly.** Source L90, “nervous system … designed to … predict, update, and conserve energy”; current row is `CONTESTED-AS-FACT / N`. Recode to `INVERTED / N`: the nervous system is made author of cognitive prediction and updating.
9. **Pure software imagery is not a math reference.** Delete current L56 `math / DECORATIVE` row “compiles it into JSON.” It is a metaphor containing no mathematical structure. Retain the RPE equation, fractal claim, literal gravitational claim, and Kha-Ba-La row.
10. **Load duplicates.** Current L52 “layering certainty…” and “hierarchy collapses…” rows state one neural-collapse mechanism and are both `Y`; keep at most one `Y`. The historical Schultz row becomes `N` as above.

**Corrected minimum:** science 42 (`ALIGNED 3, GROUNDED-OBSERVATIONAL 10, AUTHORITY-BORROWED 6, CONTESTED-AS-FACT 16, FABRICATED 2, INVERTED 5`); math 4 (`INTEGRATED 2, DECORATIVE 1, WRONG 1`); dross 31; failing load at most 11; **Nigredo remains MAJOR DROSS**. Two fabrications now independently preserve later Rubedo `ESCALATE-TO-MANUAL`; this does not alter the recovered Nigredo threshold.

## 6. `the-downstream-mind.md` — **FAIL**

1. **Accurate experiment history is omitted from the split.** Add L282 “In the 1990s, Wolfram Schultz discovered…” and L284 “tracking dopamine neurons in … macaques” as `science / ALIGNED / N` historical rows; retain the neural-result content as `AUTHORITY-BORROWED`.
2. **L286 hides an unsupported uncertainty claim.** Keep “fired at the moment of prediction” as `AUTHORITY-BORROWED`; add “reward it wasn't yet sure of receiving” as `CONTESTED-AS-FACT / N`. Cue prediction and reward uncertainty are not the same result.
3. **A declared analogy is wrongly treated as mechanism.** Source L292 begins “Think of dopamine … as a consciousness aperture”; current row is `INVERTED / Y`. Delete that row: “Think of … as” invokes the honest-metaphor safe harbor and contains no independent science claim. Keep the later literal “at the neurochemical level … narrow the aperture” row as `CONTESTED-AS-FACT / Y`.
4. **Framework existence is conflated with framework truth.** Current L298 “maps the nine Greek Muses to nine endocrine functions” is `CONTESTED-AS-FACT / N`. Recode the existence of that framework to `ALIGNED / N`; add separate `CONTESTED-AS-FACT / N` rows for “ancient taxonomy … rendered in biochemical terms” and the Thalia–dopamine correspondence.
5. **List items are mistaken for operated mathematics.** Delete current L392 `DECORATIVE` rows “Vedic astronomical computation,” “biorhythmic modelling,” and “sacred geometry.” The passage explicitly calls them lenses and shows no science claim or mathematical structure. Safe-harbor framing is not a failing verdict.
6. **The endocrine paragraph omits one inverted arrow.** Add L426 “Cortisol … intensifies the field” as `science / INVERTED / N`. Keep the non-deterministic dopamine and oxytocin statements `ALIGNED`.
7. **Explicit model framing is ignored.** Source L484 says “There is a model of social consciousness…”; current row is `CONTESTED-AS-FACT / N`. Recode to `GROUNDED-OBSERVATIONAL / N` under the gate's explicit-model rule. The later definite claim “trust circle notices” correctly remains contested.
8. **Symbolic circle imagery is not mathematics.** Delete current L490 `DECORATIVE` row “the serpent completing the circle”; it contains no mathematical structure.
9. **The quine error is not load-bearing.** Current L478 `WRONG / Y` correctly rejects propagation as a quine, but change load to `N`: removing the closing quine analogy does not collapse the Easter-Egg-Economy thesis.
10. **Every 50/50 repetition cannot be `Y`.** Keep the first numerical thesis row L228 `WRONG / Y`. Change repeated math rows L294, L296, L352, and L448 to `N`, and repeated science rows L294, L296, and L352 to `N`; removing any one repetition leaves the same numerical premise elsewhere. Change closing authority-stack L504 to `N` for the same reason.

**Corrected minimum:** science 52 (`ALIGNED 11, GROUNDED-OBSERVATIONAL 3, AUTHORITY-BORROWED 10, CONTESTED-AS-FACT 24, FABRICATED 1, INVERTED 3`); math 10 (`INTEGRATED 0, DECORATIVE 4, WRONG 6`); dross 48; failing load at most 17; **Nigredo remains MAJOR DROSS**.

## 7. `the-ineffable-secrets-of-a-breathing-sprite.md` — **FAIL**

1. **House-tradition content is under-extracted.** Add five `science / ALIGNED / N` rows for the independent L51 vayu mappings (prāṇa/intake, apāna/elimination, samāna/metabolic integration, udāna/upward movement, vyāna/distribution). Add L55 “Prana-vayu was running dominant; apana-vayu was suppressed” as `ALIGNED / N`; it is the named house model applied to direct observation.
2. **Pure software metaphor is being inventoried as science.** Delete current L43 row “pulmonary circuit runs on compiled firmware”; the independent false medulla/gestation claim remains `CONTESTED-AS-FACT`. Delete current L79 row “system does not accept pull requests”; the preceding immutable-embryonic-binaries claim remains `CONTESTED-AS-FACT`. These phrases contain no independent scientific claim.
3. **A physics reference is double-counted as math.** Keep current L65 science row for merging background radiation and quantum-vacuum fluctuation into a universal dark current. Delete the separate L65 `math / DECORATIVE` row “quantum vacuum fluctuation”: it is physics vocabulary, not a mathematical concept.
4. **The central false anatomy is repeatedly marked load-bearing.** Source L11 is frontmatter repetition of the body thesis, so change its `Y` to `N`. Keep the first body exclusivity/innervation claims at L45 load-bearing. Change repeated rows L47 “dual nerve supply,” L81 “only interrupt,” L81 “dual citizenship,” L81 “literal anatomy … atlas,” and L81 “only muscle with this dual innervation” to `N`; removing any one repeat leaves the same false anatomy stated several times.

**Corrected minimum:** science 44 (`ALIGNED 9, GROUNDED-OBSERVATIONAL 8, AUTHORITY-BORROWED 2, CONTESTED-AS-FACT 16, FABRICATED 9, INVERTED 0`); math 4 (`INTEGRATED 1, DECORATIVE 3, WRONG 0`); dross 30; failing load at most 6; **Nigredo remains MAJOR DROSS**. Nine fabrication rows preserve the separate later Rubedo manual-escalation route.

## 8. `the-source-code-has-authors.md` — **FAIL**

1. **Accurate framework attribution is misrouted as borrowed authority.** Source L49 introduces “Tsarion's concept of psychic incest”; current row L49 is `AUTHORITY-BORROWED / N`. Recode the attributed concept to `ALIGNED / N`; it accurately reports the named framework, while its house “inherited code” crosswalk is separately framed. Source L63 explicitly says Montalk “framed the inquiry in terms” of projected false reality; current row is `AUTHORITY-BORROWED / N`. Recode to `ALIGNED / N` as an accurate statement of his model, not a proof that the model is settled reality.
2. **Conditional model framing is wrongly failed.** Source L57, “If nature is habitual”; current row is `AUTHORITY-BORROWED / N`. Delete it. The conditional is safe-harbor framing and makes no independent factual claim; the preceding asserted morphogenetic-information and field-intervention claims correctly remain contested.
3. **A name list is not an authority claim.** Source L71 lists thinkers and says they are “all here”; current row is `AUTHORITY-BORROWED / N`. Delete the row. No scientific claim is attributed to the names and no conclusion is proved by credentials in that sentence.
4. **Software metaphors are misinventoried as neuroscience.** Delete current science rows L79 “nervous system running inherited code” and L83 “nervous system is running code you did not write.” They are the post's declared source-code metaphor and contain no measurable neural code claim. The actual biological/morphic assertions remain separately inventoried.
5. **Whitehead's accurate process frame is under-extracted.** Add `science / ALIGNED / N` rows for L55 “actual entities … are … drops of experience” and “each one a process of becoming.” These are accurate statements of the named, consciousness-primary process model. The untraceable direct quotation correctly remains `FABRICATED`.
6. **A real math term is omitted while a mere count is over-inventoried.** Add L35 “Polymath Compass maps four quadrants” as `math / DECORATIVE / N`: no axes or quadrant construction is shown and the post survives its removal. Delete current L57 “sixteen micro-rituals across four spins”; those are inventory counts, not a mathematical operation or structure.
7. **Repeated load is counted multiple times.** Current L49 science and math precessional-encoding rows describe the same unsupported map; keep only the math failure `Y` and change the science row to `N`. Change repeated L79 precessional row to `N`. Of the two L57 morphic-field `Y` rows, keep only the first `Y`; of the two L59 universal birth-trauma `Y` rows, keep only the first. Removing a second paraphrase does not collapse the lineage argument.

**Corrected minimum:** science 27 (`ALIGNED 12, GROUNDED-OBSERVATIONAL 0, AUTHORITY-BORROWED 1, CONTESTED-AS-FACT 12, FABRICATED 2, INVERTED 0`); math 7 (`INTEGRATED 1, DECORATIVE 3, WRONG 3`); dross 21; failing load at most 7; **Nigredo remains MAJOR DROSS**. The two fabrication findings still independently trigger later Rubedo manual escalation.

## 9. `the-sun-names-you.md` — **FAIL**

1. **The solar-core account is passed despite a false mechanism.** Source L44 and repeated L48 say gravity compresses hydrogen “until the Coulomb barrier breaks”; both current rows are `GROUNDED-OBSERVATIONAL / N`. Recode both to `CONTESTED-AS-FACT / N`: stellar fusion proceeds through quantum tunnelling at energies below the classical Coulomb barrier; the barrier does not simply break.
2. **A second solar error is omitted.** Source L50, “sustained combustion chamber”; no current row. Add `science / CONTESTED-AS-FACT / N`: the Sun is not chemically combusting.
3. **Accurate intellectual history is incompletely inventoried.** Add L68, “Vaiśeṣika school treats [sight] as an atomistic assembly,” as `science / ALIGNED / N`, parallel to the current Nyāya row.
4. **Psychological and neural mechanisms are under-split.** Add L70, “self-model runs unchecked … in the absence of external data,” as `CONTESTED-AS-FACT / N`; L74, “brain is forced to integrate an external witness,” as `INVERTED / N`; L76, “self-model is a prediction machine,” as `CONTESTED-AS-FACT / N`; and “recognition … feels like a threat … because [of this neural sequence]” as `CONTESTED-AS-FACT / N`.
5. **House-cosmology science is omitted.** Add `ALIGNED / N` rows for L84 “anandamaya kosha is the fifth and most subtle sheath,” L84 “registers structural coherence,” and L86 “Kena Upaniṣad states that awareness cannot know itself as an object.” These are named and operated house structures.
6. **Declared lens geometry is over-inventoried.** Delete current math row L68 “different curvatures, different focal lengths.” The sentence explicitly compares philosophical seeing-systems as lenses and contains no actual geometric structure.
7. **The earlier Kha-Ba-La construction is omitted.** Add L80, “the three reach equilibrium … illumination … body … resistance,” as `math / INTEGRATED / Y`. Change the later L88 integrated triad row to `N`, because it repeats that mapping.
8. **Repeated literal-correspondence and neural conclusions cannot all be `Y`.** At L44, “same grammar” and “This is not a metaphor” are the same upgrade; keep only one `Y`. Change the repeated L48 “This is not a metaphor” row to `N`. Change concluding L92 “ACC fire, DMN resist” to `N`; it repeats the L74/L76 claims.

**Corrected minimum:** science 41 (`ALIGNED 7, GROUNDED-OBSERVATIONAL 7, AUTHORITY-BORROWED 0, CONTESTED-AS-FACT 23, FABRICATED 1, INVERTED 3`); math 6 (`INTEGRATED 2, DECORATIVE 4, WRONG 0`); dross 31; failing load at most 6; **Nigredo remains MAJOR DROSS**.

## 10. `the-tower-speaks-in-richter-scale.md` — **FAIL**

1. **The fabrication verdict is supported, but duplicate load is wrong.** Source L11 and L25 repeat “A 6.0 earthquake — Bangkok's strongest in living memory”; both current rows are `FABRICATED / Y`. The official USGS event record identifies the Bangkok-damaging 28 March 2025 earthquake as the M7.7 Mandalay, Myanmar event, while the post metadata is dated 15 March. Keep both fabrication rows, but change frontmatter L11 to `N` and retain body L25 as `Y`; deleting the excerpt does not collapse the narrative. Evidence: [USGS M7.7 event](https://earthquake.usgs.gov/earthquakes/eventpage/us7000pn9s).
2. **The closing observation repeats earlier evidence.** Source L35 “body registered before the mind could name it”; current row is `GROUNDED-OBSERVATIONAL / Y`. Change to `N`, because L29 already carries the observation and removing the conclusion leaves it intact.
3. **A symbolic spiral is not a math reference.** Delete current L37 `math / DECORATIVE` row “Every spiral begins with a rupture.” It is a closing metaphor with no curve, recurrence, or mathematical structure; the analogy should not be converted into an inventory row.

**Corrected minimum:** science 8 (`ALIGNED 1, GROUNDED-OBSERVATIONAL 5, AUTHORITY-BORROWED 0, CONTESTED-AS-FACT 0, FABRICATED 2, INVERTED 0`); math 3 (`INTEGRATED 0, DECORATIVE 3, WRONG 0`); dross 5; failing load 1; **Nigredo remains MAJOR DROSS** because any fabrication is a recovered Nigredo major trigger. The two separate source occurrences still preserve the later Rubedo ≥2-fabrication manual route; load deduplication does not merge inventory rows.

## 11. `vimshottari-dasha-markov-chains.md` — **FAIL**

1. **The epigraph fabrication is missed.** Source L31–L32 attributes “The universe does not roll dice…” to “Pattern Recognition Protocol”; the current audit has only a `math / DECORATIVE` row. Repository-wide exact search finds no traceable protocol/source for the quotation. Add `science / FABRICATED / N`; keep the mathematical slogan row separately.
2. **L36 bundles modern reinterpretation with a false date.** Current science row L36 quotes only “encoded precisely this architecture” but its note also mentions “roughly four thousand years.” Split it: retain `CONTESTED-AS-FACT / Y` for precise Markov encoding and add `FABRICATED / N` for the unsupported four-thousand-year historical attribution.
3. **A duration table is not a transition matrix.** Source L49 “This is a transition matrix”; current row is `math / INTEGRATED / Y`. Recode to `WRONG / N`: at that point the post has shown only a dictionary of holding times. The later 0/1 successor array at L68 remains a valid embedded transition matrix and load-bearing evidence.
4. **Declared microservice analogy is over-inventoried.** Delete current L77 `DECORATIVE` row “microservices within microservices” and L79 `DECORATIVE` row “scope and closure.” L77 explicitly begins “Think of it as”; neither phrase contains an actual mathematical structure.
5. **The implementation contradicts the initial-condition claim.** Add L88–L97 as `math / WRONG / N`: `birth_pada` is unused, no remaining birth-dasha balance is calculated, and the code appends the full starting period despite L85's rule.
6. **Two trajectory claims are omitted.** Add L100 “entirely different dasha sequences” as `WRONG / N`: changing the starting lord rotates the same fixed cycle, not an entirely different transition law. Add L104 “every planetary influence … exactly once per 120-year revolution” as `WRONG / N`: a lifecycle beginning partway through a dasha has partial repeated endpoint occupancy even though the complete lord schedule totals 120 years.
7. **The same Markov-identity failure is repeatedly load-bearing.** Keep the frontmatter L4 central failure `Y`; change later L49 “not metaphor,” L110 “parallel is not decorative,” and L116 “perfectly respectable Markov chain” to `N`. They repeat the identity claim and their removal leaves it elsewhere.

**Corrected minimum:** science 12 (`ALIGNED 6, GROUNDED-OBSERVATIONAL 0, AUTHORITY-BORROWED 0, CONTESTED-AS-FACT 2, FABRICATED 4, INVERTED 0`); math 32 (`INTEGRATED 11, DECORATIVE 4, WRONG 17`); dross 27; failing load at most 14; **Nigredo remains MAJOR DROSS**. Four fabrications preserve the separate later Rubedo manual-escalation route.

## 12. `vortex-based-mathematics.md` — **FAIL**

1. **L43 must be split as history.** Current science row “Brahmagupta formalized zero and the place-value notation in the 7th century” is `CONTESTED-AS-FACT / N`. Replace it with three rows: `ALIGNED / N` for Brahmagupta's seventh-century arithmetic treatment of zero; `FABRICATED / N` for attributing decimal place-value notation's invention/formalization to him; and `FABRICATED / N` for the invented accounting motive (“grain ledgers, tax receipts, temple inventories”).
2. **The torus mapping omits an independent false leg.** Add L55 “3-6 oscillation maps onto the toroidal rotation” as `math / WRONG / N`; the current row only covers the poloidal assignment.
3. **L57 is not atomically split.** Keep current `WRONG / N` row “dimensional reduction that hides the knot,” and add a separate `WRONG / N` row for “The number line … is a projection — one coordinate … flattened.” A number line is not a coordinate projection of this digital-root orbit.
4. **A field-symmetry assertion needs its math verdict.** Source L61 “resulting field should carry the symmetries of the circuit” is currently only `science / CONTESTED-AS-FACT / N`. Add `math / WRONG / N`: no representation or electromagnetic symmetry map is defined.
5. **Declared Tarot lens is not mathematics.** Delete current L79 `DECORATIVE / N` row “pillars are the doubling and halving circuits.” The paragraph explicitly begins “Read through this lens”; it is symbolic analogy, and no halving circuit or mathematical correspondence is demonstrated.
6. **Three closing paraphrases cannot all be load-bearing.** Current L83 rows “draws a shape…,” “torus is not decorative,” and “kinematic signature…” are all `WRONG / Y`. Keep at most the first `Y`; change the other two to `N` because each is a restatement of the same failed universal-torus conclusion.

**Corrected minimum:** science 15 (`ALIGNED 3, GROUNDED-OBSERVATIONAL 4, AUTHORITY-BORROWED 0, CONTESTED-AS-FACT 6, FABRICATED 2, INVERTED 0`); math 33 (`INTEGRATED 9, DECORATIVE 2, WRONG 22`); dross 32; failing load at most 17; **Nigredo remains MAJOR DROSS**. The two historical fabrications also create the separate later Rubedo manual-escalation route.

## 13. `water-fourth-phase.md` — **FAIL**

1. **Accurate publication history is misrouted as borrowed authority.** Source L44 “Pollack's 2013 monograph *The Fourth Phase of Water*”; current row is `AUTHORITY-BORROWED / N`. Recode the bibliographic/content statement to `ALIGNED / N`; the monograph-derived H₃O₂, EZ, potential, and conductivity claims remain separately borrowed/contested.
2. **The frontmatter compound claim is under-split.** Besides the existing solvent, liquid-crystal, and consciousness rows, add separate `CONTESTED-AS-FACT / N` rows for L11 “charge-separating,” “information-storing,” “coherent-domain-forming,” and “fourth phase between solid and liquid … interface.” These are four independently falsifiable physical claims.
3. **The hero's information mechanism is omitted.** Add L37 “physical substrate through which information enters biological form” as `science / CONTESTED-AS-FACT / N`, distinct from the current liquid-crystal-antenna row.
4. **Levin history and the claimed Pollack continuity are conflated.** Current L50 row “Michael Levin's lab at Tufts has since demonstrated its experimental consequence” is `AUTHORITY-BORROWED / N`. Split it into `ALIGNED / N` for the accurate lab/history attribution and `CONTESTED-AS-FACT / N` for the unsupported claim that Levin demonstrated Ho/Pollack fourth-phase consequences. The following substantive bioelectric claim remains `AUTHORITY-BORROWED`.
5. **Two body-mechanism claims are omitted.** Add L50 “gel matrix … computes through charge topology” as `CONTESTED-AS-FACT / N`; add L52 “fascial network … body's primary signaling system” as `FABRICATED / N`, separate from the already inventoried unsupported speed/globality superlative.
6. **Three metaphors are not math inventory.** Delete current math rows L48 “spiraling channels of low resistance,” L56 “We are the local topology,” and L56 “water became recursive enough to map itself.” The first is a physical-shape report already covered by science; the latter two are declared cross-scale metaphors with no actual topology or recursive rule.
7. **The same water/material-first thesis is repeatedly marked `Y`.** Change excerpt L11 “Water is not a solvent,” L11 “liquid crystal semiconductor,” and L11 consciousness-causation rows to `N`; retain first full body statements as load-bearing. Change repeated hero L37 antenna, L50 “gel is fourth phase,” L52 “OS is written in water,” L56 material-propagation consciousness, and L60 “grammar by which life…” to `N`. Removing any one repetition leaves the thesis stated elsewhere.

**Corrected minimum:** science 63 (`ALIGNED 4, GROUNDED-OBSERVATIONAL 3, AUTHORITY-BORROWED 9, CONTESTED-AS-FACT 34, FABRICATED 8, INVERTED 5`); math 3 (`INTEGRATED 1, DECORATIVE 2, WRONG 0`); dross 58; failing load at most 6; **Nigredo remains MAJOR DROSS**. Eight fabrication findings preserve the separate later Rubedo manual route.

## 14. `your-reality-is-a-smart-contract.md` — **FAIL**

1. **A house-history claim is wrongly left as borrowed authority.** Source L78 “Haṭha Yoga tradition states that every cell is connected and all records are preserved”; current row is `AUTHORITY-BORROWED / N`. Recode to `FABRICATED / N`: the specific universal cellular-record attribution is untraceable and should not borrow the house tradition's name. Add L86's accurate Arjuna/Kṛṣṇa textual application as `ALIGNED / N`; the claimed nervous-system recompilation remains separately contested.
2. **Deployed bytecode is misclassified.** Source L48 “write immutable logic onto a distributed ledger”; current row is `math / WRONG / N` because proxies can upgrade behavior. Recode to `INTEGRATED / N`: deployed bytecode at an address is durable/immutable even when proxy indirection changes which implementation future calls reach. The separate universal-irreversibility row remains wrong.
3. **The demonstrated compiler mapping is contradicted by its own row verdicts.** Current L58 compiler-pipeline row is `INTEGRATED / Y`, but L60 Para/specification, L62 Pashyanti/AST, L64 Madhyama/source, and L66 Vaikhari/binary are all `DECORATIVE`. Recode those four child rows to `INTEGRATED / N`: they are the explicit step-by-step structural correspondence that makes the parent pipeline integrated. The false “cannot be uncompiled” child remains `WRONG`.
4. **Nine software metaphors contain no independent math structure.** Delete current `DECORATIVE` rows L68 “cron job,” L68 “dependency management,” L76 “state mutation,” L78 “posture is a state variable,” L84 “kosha stack,” L86 “Reverse-engineering,” both L94 “state verification function” and “transaction/receipt,” and L104 “functions … loops that update nothing.” The underlying ritual, physiology, Tarot, and behavioral claims are already inventoried; these labels alone are analogy, not math.
5. **The one trigger sequence that claims formal structure is taxonomized too softly.** Source L80 “The input arrives. The function executes. The state updates”; current row is `DECORATIVE / N`. Recode to `WRONG / N`: the paragraph insists the personal response is deterministic, but supplies no state space or transition function and its science row already rejects exceptionless execution.
6. **Duplicated load crosses taxonomies.** Current L48 “transmit logic into the nervous system” and “transaction is irreversible” are two rows for one initiation mechanism; keep only the first `Y`. At L88 the science claim about two-second/three-millimetre changes and the math Lorenz bridge cover the same unsupported mapping; keep the science row `Y` and change math to `N`.

**Corrected minimum:** science 35 (`ALIGNED 8, GROUNDED-OBSERVATIONAL 4, AUTHORITY-BORROWED 0, CONTESTED-AS-FACT 16, FABRICATED 4, INVERTED 3`); math 23 (`INTEGRATED 8, DECORATIVE 6, WRONG 9`); dross 38; failing load at most 10; **Nigredo remains MAJOR DROSS**. Four fabrications preserve the separate later Rubedo manual-escalation route.

## Cross-slice conclusion

- **PASS posts:** 0/14. **FAIL posts:** 14/14.
- Quote anchors, table shape, category labels, and recovered `CLEAN` / `MINOR DROSS` / `MAJOR DROSS` arithmetic can pass while the semantic audit still fails. No finding above changes the intentional Nigredo thresholds.
- The dominant defects are: atomic claims collapsed into one row; accurate history misrouted to `AUTHORITY-BORROWED`; explicit models/declared lenses converted into failures; pure analogy invented as science/math inventory; correct but removable math promoted to `INTEGRATED`; and repeated thesis statements each marked `Y` despite the removal test.
- Fabrication routing is downstream-only: `>=2 FABRICATED` findings affects later Rubedo `ESCALATE-TO-MANUAL`; Nigredo remains governed by the recovered rule (`any FABRICATED`, at least three failing rows, or at least two load-bearing failures => `MAJOR DROSS`).

## Resolution verification

Independent closure pass against the complete current sources, current audits, repair receipt, and canonical gate. The receipt's requested repairs are present, and all quote-anchor and taxonomy-invariant scripts pass; the verdicts below additionally re-run the canonical §4.1/§4.4 removal test on every current `INTEGRATED` row. A correct but removable or duplicated reference is `DECORATIVE / N`, never `INTEGRATED / N` or `INTEGRATED / Y`.

1. **FAIL — `seventeen-ways-pattern-repeats.md`.** The requested repair is present, but six current `INTEGRATED / Y` rows fail removal: L59 “There exist exactly seventeen…” is an epigraph duplicated by the body theorem; L83's Penrose example is removable from the periodic/forbidden-order argument; L111's p1/p6mm point-group comparison and L113's unexpanded `1 + 1 + 4 + 3 + 5 + 3` arithmetic are supporting details removable from the classification; L133 repeats the L81 restriction; and L151 repeats the seventeen-group conclusion. Recode all six to `DECORATIVE / N`. Corrected minimum: science 18 unchanged; math 31 (`INTEGRATED 5, DECORATIVE 8, WRONG 18`); dross 39; failing load 23; `MAJOR DROSS`.
2. **FAIL — `shadbala-tensor-field-theory.md`.** Every requested semantic split is present, but current L30 “a vector in six-dimensional space” is an epigraph duplicated by the body representation at L35; deleting it leaves the vector argument intact. Recode L30 from `INTEGRATED / Y` to `DECORATIVE / N`. Corrected minimum: science 30 unchanged; math 53 (`INTEGRATED 1, DECORATIVE 14, WRONG 38`); dross 71; failing load 27; `MAJOR DROSS`.
3. **PASS — `spolski-endocrine-framework.md`.** All requested atomic splits, omitted mechanisms, mixed-clause verdicts, one-failing-load-per-pair deduplication, and final totals are present and semantically consistent; its sole math row is correctly `DECORATIVE / N`.
4. **PASS — `sri-yantra-geometry-that-doesnt-fit.md`.** The history, rosette, D9-consequence, planar/space-group, experience, and load repairs are present; canonical §4.1 is correctly applied by routing removable correct counts/restrictions to `DECORATIVE / N`, while the four surviving `INTEGRATED / Y` rows are distinct structural legs.
5. **PASS — `the-devil-in-the-detail.md`.** The Schultz-history split, RPE substrate, conditioning and observation splits, pratyāhāra and urge-surfing routing, self-model and nervous-system verdicts, JSON deletion, load deduplication, and totals all match the source and gate; both remaining integrated structures survive removal testing.
6. **PASS — `the-downstream-mind.md`.** Every required history/model/analogy deletion or recode and every duplicated-load correction is present; the final inventory contains no `INTEGRATED` math row, and its `DECORATIVE`/`WRONG` routing matches §4.1.
7. **PASS — `the-ineffable-secrets-of-a-breathing-sprite.md`.** The five-vayu split, applied observation, software-metaphor deletions, quantum-vacuum type correction, repeated-anatomy load deduplication, fabrication count, and totals are present; the sole Kha-Ba-La math structure remains demonstrably operated.
8. **PASS — `the-source-code-has-authors.md`.** Framework/history routing, conditional and name-list deletions, software-metaphor removal, Whitehead split, quadrant/count changes, cross-taxonomy load deduplication, and totals are correct; the explicit triad removal construction is the only integrated math row.
9. **PASS — `the-sun-names-you.md`.** The fusion/combustion errors, Vaiśeṣika history, neural splits, house-cosmology rows, lens deletion, triad placement, duplicate-load changes, and canonical `INTEGRATED / Y` versus repeated `DECORATIVE / N` result are all present and correct.
10. **PASS — `the-tower-speaks-in-richter-scale.md`.** Both fabrication occurrences remain inventoried with only the body occurrence load-bearing, the repeated observation is `N`, the spiral metaphor is absent from math inventory, and the summary preserves recovered Nigredo and later Rubedo routing separately.
11. **FAIL — `vimshottari-dasha-markov-chains.md`.** The requested seven repairs are present, but two current conclusion rows remain falsely integrated: removing L116 “a formal specification of temporal state evolution” leaves the already demonstrated fixed schedule/state representation intact, and removing L120 “the transition matrix beneath the tradition” leaves the actual L68 matrix and L55 embedded-chain construction intact. Recode both from `INTEGRATED / Y` to `DECORATIVE / N`. Corrected minimum: science 12 unchanged; math 32 (`INTEGRATED 9, DECORATIVE 6, WRONG 17`); dross 29; failing load 14; `MAJOR DROSS`.
12. **FAIL — `vortex-based-mathematics.md`.** The requested history and mathematical splits are present, but three correct repetitions fail removal: L11's frontmatter orbit duplicates the body derivation, L47 “a power of two reduced to its digital root” restates the already demonstrated six-cycle, and L53's three-regime summary repeats the L45/L51 component results. Recode those three `INTEGRATED / Y` rows to `DECORATIVE / N`. Corrected minimum: science 15 unchanged; math 33 (`INTEGRATED 6, DECORATIVE 5, WRONG 22`); dross 35; failing load 17; `MAJOR DROSS`.
13. **PASS — `water-fourth-phase.md`.** Every required history split, frontmatter/hero/body extraction, Levin separation, omitted mechanisms, metaphor deletion, repeated-load correction, fabrication routing, and total is present; the single triad removal construction remains structurally integrated.
14. **FAIL — `your-reality-is-a-smart-contract.md`.** The receipt correctly applies §4.1 to bytecode and the four compiler children, but the remaining smart-contract definition is anchored only to frontmatter L11 as `INTEGRATED / Y`; deleting that excerpt leaves the verbatim body definition at L40, so the L11 occurrence is removable. Recode L11 to `DECORATIVE / N` and add L40 “A smart contract is code that executes deterministically when its conditions are met” as `INTEGRATED / Y`. Corrected minimum: science 35 unchanged; math 24 (`INTEGRATED 3, DECORATIVE 12, WRONG 9`); dross 44; failing load 10; `MAJOR DROSS`.

**Closure count:** PASS 9/14; FAIL 5/14. Every post remains `MAJOR DROSS`; these residual category/load corrections do not alter recovered Nigredo thresholds or the separate later Rubedo fabrication route.

### Targeted recheck

Independent recheck of the five targeted repairs against the current audits, exact source lines, the receipt addendum, and canonical §4.1/§4.4 removal semantics:

1. **PASS — `seventeen-ways-pattern-repeats.md`.** L59, L83, L111, L113, L133, and L151 are now `DECORATIVE / N`. The five surviving `INTEGRATED / Y` rows provide distinct structural legs: periodic-plane scope, group definition, crystallographic restriction, full seventeen-group enumeration, and the flat-versus-hyperbolic branch. Final science 18; math 31 (`INTEGRATED 5, DECORATIVE 8, WRONG 18`); dross 39; failing load 23; `MAJOR DROSS`.
2. **PASS — `shadbala-tensor-field-theory.md`.** The duplicated L30 epigraph is now `DECORATIVE / N`; L35 is the sole `INTEGRATED / Y` body anchor for the six-coordinate vector representation. Final science 30; math 53 (`INTEGRATED 1, DECORATIVE 14, WRONG 38`); dross 71; failing load 27; `MAJOR DROSS`.
11. **PASS — `vimshottari-dasha-markov-chains.md`.** L116 and L120 are now `DECORATIVE / N`. The nine surviving `INTEGRATED / Y` rows separately establish the finite-state-machine frame, canonical durations and cycle, Markov definition and memorylessness, deterministic embedded chain and successor matrix, starting condition, and nonabsorbing/nontransient cycle property. Final science 12; math 32 (`INTEGRATED 9, DECORATIVE 6, WRONG 17`); dross 29; failing load 14; `MAJOR DROSS`.
12. **PASS — `vortex-based-mathematics.md`.** The duplicated L11 orbit, L47 power-of-two gloss, and L53 three-regime summary are now `DECORATIVE / N`. The six surviving `INTEGRATED / Y` rows are the demonstrated six-cycle, group identification, two-cycle, fixed point, torus two-cycle fact, and twelve-step fifths closure. Final science 15; math 33 (`INTEGRATED 6, DECORATIVE 5, WRONG 22`); dross 35; failing load 17; `MAJOR DROSS`.
14. **PASS — `your-reality-is-a-smart-contract.md`.** The duplicated frontmatter definition at L11 is now `DECORATIVE / N`, and its body occurrence at L40 is `INTEGRATED / Y`. The three surviving integrated structures are the body smart-contract definition, operated four-stage compiler pipeline, and explicit Kha-Ba-La removal construction. Final science 35; math 24 (`INTEGRATED 3, DECORATIVE 12, WRONG 9`); dross 44; failing load 10; `MAJOR DROSS`.

All five quote-anchor checks passed with zero failures over 49, 83, 44, 48, and 59 checked rows respectively. All five taxonomy-invariant checks also passed with zero failures over the same row counts.

**Updated aggregate closure:** PASS 14/14; FAIL 0/14. No semantic residual remains in this slice; every post retains its independently verified `MAJOR DROSS` Nigredo verdict, and later Rubedo fabrication routing remains separate.
