# Nigredo semantic review — manifest posts 15–28

**Reviewer scope:** `quality-engine/manifests/nigredo-remaining-42.json` → `posts[14:28]`
**Canonical gate:** Fool's Wisdom Grounding Gate v2.2.0
**Review type:** semantic only; source posts and current audits were read-only
**Threshold note:** CLEAN / MINOR DROSS / MAJOR DROSS below are the recovered Nigredo thresholds. They are intentionally separate from later Rubedo routing such as `ESCALATE-TO-MANUAL`.

## 1. `lorenz-kundli-pattern-hub.md` — FAIL

The audit reaches the right `MAJOR DROSS` threshold, but its inventory, history routing, and load-bearing flags are not semantically reliable.

- **History rule:** Source L32 (`"In 1963, Edward Lorenz published"`) is current audit row L10 `GROUNDED-OBSERVATIONAL`; L32 (`"As James Gleick documented"`) is row L12 `GROUNDED-OBSERVATIONAL`; L34 (`"spherical astronomy of the Surya Siddhanta"`) is row L15 `GROUNDED-OBSERVATIONAL`; and L66 (`"documented in the Brihat Parashara Hora Shastra"`) is row L25 `GROUNDED-OBSERVATIONAL`. These are inventoried as accurate history, so the task's explicit history rule requires **recode all four to `ALIGNED`**, preserving the Notes as historical-status explanations.
- **Fabrication routing:** Source L34 says observers three thousand years earlier were `"solving the same problem"`. Current row L14 calls this `CONTESTED-AS-FACT`, but it is a retrospective intellectual-priority assertion for which no historical identity of problems is supplied. **Recode row L14 to `FABRICATED`**, not a merely contested physical model.
- **Exhaustiveness / compound math omission:** Source L110 says the 27 mansions and their subdivisions invite analysis through both `"golden ratio"` and `"recursive sequence frameworks"`; there is no L110 audit row. **Add two separate math rows**, one per independently removable concept, both `DECORATIVE / N` unless the audit can show a ratio or recurrence.
- **Compound Lorenz claim omitted:** Source L56 independently claims a nonrepeating trajectory, determinism, and unpredictability. Current row L19 inventories only the phase-space point. **Split the remaining Lorenz claims into separate rows** (at minimum, deterministic evolution and sensitive unpredictability) and judge each occurrence by removal rather than silently absorbing them into row L19.
- **Duplicated-load inflation:** Source L4's advertised parallel is current row L9 `WRONG / Y`, but the same thesis recurs at L60, L68, L76, L88, L127, and L129. Removing the frontmatter occurrence leaves the argument intact. **Change row L9 to `WRONG / N`** and re-run the same occurrence-level test on the repeated exact-parallel/isomorphism rows; duplicated statements cannot all be independently load-bearing.

Corrected minimum science totals, before adding any newly split L56 science content: **6** (`ALIGNED 4`, `GROUNDED-OBSERVATIONAL 1`, `AUTHORITY-BORROWED 0`, `CONTESTED-AS-FACT 0`, `FABRICATED 1`, `INVERTED 0`). Corrected minimum math totals after the two missing L110 rows: **35** (`INTEGRATED 7`, `DECORATIVE 10`, `WRONG 18`). Nigredo remains **MAJOR DROSS**; the single fabrication does not by itself establish the gate's later ≥2-fabrication Rubedo escalation trigger.

## 2. `lorenz-kundli-protocol.md` — FAIL

The mathematical diagnosis is broadly strong and the Nigredo verdict remains `MAJOR DROSS`, but the audit misses independent biological and historical claims, misroutes clear fabrications, and overstates duplicated load.

- **One sentence, three biological claims:** Source L83 independently asserts `"endocrine shift"`, `"sleep fragmentation"`, and `"digestive change"` from three astrological configurations. Current row L53 inventories only the endocrine claim and marks it `CONTESTED-AS-FACT / Y`. **Split L83 into three science rows**. All three are at least `CONTESTED-AS-FACT`; each should be `N` under the occurrence removal test because either of its siblings and the surrounding Ba argument survive its deletion.
- **False historical-priority claim omitted:** Source L71 calls Ashtakavarga `"principal component analysis performed by hand, centuries before Pearson"`. Current row L46 correctly marks the PCA identity `WRONG`, but the separate priority/history claim is absent. **Add a science row at L71 as `FABRICATED / N`**; there is no PCA operation here to have preceded Pearson.
- **Model reified as physical history:** Source L93 says the Lorenz attractor `"has cycled through its lobes billions of years"`. Current row L58 calls this `CONTESTED-AS-FACT / N`, although its own Note correctly says the mathematical model is not a physical object. **Recode row L58 to `FABRICATED / N`.** The same sentence's `"since the first human looked up and noticed Venus"` is current row L59 `CONTESTED-AS-FACT / N`; **recode it to `FABRICATED / N`** as an unsupported historical continuity claim.
- **Duplicated-load inflation:** Source L11 and L38 repeat `"same diagram in different notation"` and are current rows L9 and L11, both `WRONG / Y`; the body states the same thesis again at L45 (row L20). Removing either frontmatter occurrence does not collapse the post. **Change rows L9 and L11 to `WRONG / N`**, retaining the body-level L45 failure as the load-bearing articulation.
- **L83 load correction:** As above, current row L53 must change from `Y` to `N`; adding its two missing siblings as `N` avoids counting three interchangeable examples as three thesis failures.

Corrected minimum science totals: **10** (`ALIGNED 2`, `GROUNDED-OBSERVATIONAL 1`, `AUTHORITY-BORROWED 0`, `CONTESTED-AS-FACT 4`, `FABRICATED 3`, `INVERTED 0`). Math category totals remain **45** (`INTEGRATED 8`, `DECORATIVE 10`, `WRONG 27`), but load-bearing dross falls from 20 to **at most 17** from the three explicit load corrections above. Nigredo remains **MAJOR DROSS**; three fabrications require later Rubedo fabrication routing independently of the Nigredo threshold.

## 3. `lorenz-kundli-system-index.md` — FAIL

The audit catches most central category errors but stops before the research-vector section and incorrectly passes the asserted neural-network identity.

- **Fabrication routing for history:** Source L48 claims twentieth-century mathematical structures `"were operational in Vedic astronomical systems for millennia"`. Current row L10 labels this `CONTESTED-AS-FACT / Y`. This is an unsupported intellectual-priority/history claim, not merely a contested scientific model. **Recode row L10 to `FABRICATED`** (and retain `Y` only if the reviewer can show the index collapses without this occurrence; otherwise `N`).
- **Three claimed parallels collapsed to one:** Source L54 lists phase-space mapping, scale invariance, and consciousness-interface geometry as three independent core parallels. Current row L11 inventories only `"track dynamic evolution through geometric space"`. **Add separate math rows** for `"maintain pattern coherence across magnification levels"` and `"geometric representations of awareness states"`; both are `WRONG` absent a scaling transformation, invariant, metric, or state-space map.
- **False neural-network pass:** Source L110 says the aspect graph is `"structurally identical to a neural network"`. Current row L29 marks it `INTEGRATED / Y`, even though the post supplies neither numerical weights, activation functions, layers, nor a propagation rule; current row L30 then correctly observes there is no loss or trainable weight for backpropagation. **Recode row L29 to `WRONG / Y`** (or split: the bare directed-graph correspondence may pass, but the claimed neural-network identity must fail).
- **Research vectors entirely omitted:** Source L120 asks whether Lorenz dynamics predict Dasha transitions; L122 invokes `"coupled Lorenz oscillators"`; L124 proposes `"Lyapunov exponents"`, `"fractal dimension calculation"`, and `"attractor reconstruction"`. The current audit ends its inventory at L112. **Add at least five separate math rows** for these independently testable concepts. Because they are proposed but not executed, each is `DECORATIVE / N` under the removal test, not silently exempt merely because it is phrased as a research question.
- **Duplicated-load inflation:** Source L4's `"complete topology"` is row L9 `WRONG / Y`, while topology/isomorphism claims recur throughout L54–L60 and L128. Removing the excerpt leaves the index unchanged. **Change row L9 to `WRONG / N`** and apply the occurrence-level test to repeated state-space/topology claims.

Corrected minimum totals after only the corrections above: science **2** (`ALIGNED 0`, `GROUNDED-OBSERVATIONAL 0`, `AUTHORITY-BORROWED 0`, `CONTESTED-AS-FACT 1`, `FABRICATED 1`, `INVERTED 0`); math **27** (`INTEGRATED 5`, `DECORATIVE 6`, `WRONG 16`). Additional L54 splitting can only increase the math total. Nigredo remains **MAJOR DROSS**.

## 4. `magnetic-substrate.md` — FAIL

This is a high-coverage audit and its `MAJOR DROSS` verdict is unquestionably correct, but several compound sentences remain unsplit, clear falsehoods are routed as merely contested, and duplicated frontmatter claims inflate load-bearing counts.

- **False factual claims misrouted:** Source L84 says oxygen is `"the only paramagnetic gas"`; current row L28 is `CONTESTED-AS-FACT / N`. Source L120 says fish have one-chamber hearts and amphibians two; current row L65 is `CONTESTED-AS-FACT / N`. These are definitionally false biological/chemical claims rather than competing models. **Recode both rows to `FABRICATED / N`.**
- **Incorrect pass on vagal anatomy:** Source L124 says the vagus `"descends to innervate the entire gastrointestinal tract"`; current row L72 calls this `GROUNDED-OBSERVATIONAL / N`, while row L73 separately notices the inaccurate endpoint. **Recode row L72 to `FABRICATED / N`** (or split off only the valid upper-GI innervation as grounded and add a fabricated `"entire"` claim).
- **Ventricular-pathway omission:** Source L104 says the lateral ventricles `"feed into the third ventricle through the choroid plexus"`. Current row L44 inventories only the existence of two lateral ventricles, and rows L45–L47 jump to isotope claims. **Add `FABRICATED / N` for the false choroid-plexus conduit**; CSF passes through the interventricular foramina.
- **Melanin compound omission:** Source L176 claims melanin absorbs `"across the entire electromagnetic spectrum — from UV to radio"`, then transduces signals and stores epigenetic memory. Current rows L85–L86 inventory only superconduction and memory. **Add a separate `FABRICATED / N` row for the entire-spectrum claim** and another for `"transduces electromagnetic signals into cellular instructions"` unless evidence supports that distinct mechanism.
- **Event list improperly collapsed:** Source L192 independently asserts Brazilian cardiovascular surge, Godwit/bird falls, Australian/Pacific bee collapse, and southern-hemisphere fish die-offs. Current row L98 quotes only the cardiovascular claim but its Note silently covers the whole list. **Split into four science rows**, each `FABRICATED`; this adds three rows even if the existing cardiovascular row remains.
- **Evolution timeline not split:** Source L210 independently asserts magnetic causes for the Great Oxygenation Event, Cambrian diversification, mammal origins at 320 Ma, four-chamber-heart evolution, encephalization, the Permian extinction, Siberian field collapse, volcanism, oxygen loss, and 96% mortality. Current rows L114–L115 cover only the event date and one braided coupling. **Add separate rows at minimum for `"Mammals first appear (320 million years ago)"` (`FABRICATED`) and `"magnetic field collapses in what is now Siberia"` (`FABRICATED`)**; do not hide independent claims in one causal-sequence row.
- **Coordinate identity omitted:** Source L224 says pituitary and choroid-plexus melanin sit `"at the same coordinates"`; current row L125 inventories only the Ajna/pituitary claim. **Add `FABRICATED / N`** for the anatomical coordinate identity.
- **Duplicated-load inflation:** Source L4 and L63 repeat the same oxygen/melanin/vortex thesis later developed throughout L82–L242, yet current rows L9–L12 and L18–L22 mark nearly every frontmatter occurrence `Y`. **Change those duplicated frontmatter failures to `N`** unless deleting that single occurrence, with the entire body intact, collapses the argument. The current 83 load-bearing count is therefore not defensible even though the post remains overwhelmingly major.

The clear corrections above yield a minimum science count of **159**: starting from 150, two category-only recodes add no rows; the vagal recode adds no row; the ventricular claim (+1), two omitted melanin claims (+2), three extra L192 rows (+3), two L210 rows (+2), and L224 (+1) produce at least nine additions, while splitting the current L192 row preserves its original occurrence. Because further compound claims remain, an exact final total requires a row rebuild. At minimum, `FABRICATED` rises from 77 to **89** (three recodes plus nine additions) and `GROUNDED-OBSERVATIONAL` falls from 26 to **25**; `CONTESTED-AS-FACT` falls from 31 to **29**. Nigredo remains **MAJOR DROSS**, and the later Rubedo result remains fabrication-escalated.

## 5. `master-synthesis.md` — FAIL

The verdict remains `MAJOR DROSS`, but the audit treats non-mathematical labels and protocol counts as math references contrary to the task's declared-analogy/underlying-content rule.

- **Human Design label is not mathematics:** Source L88 names `"Gate 23.4 in the Human Design System"`. Current row L22 marks it `DECORATIVE / N` because it looks decimal-like. A code/label is not a mathematical concept, and no arithmetic is performed on `23.4`. **Delete row L22** rather than treating superficial numeric typography as math.
- **Breath instructions are not automatically math claims:** Source L175 gives the `7-1-3` breath protocol and L176 gives `4:4:4` box breathing. Current rows L29–L30 classify both `DECORATIVE / N` solely because they contain counts. These are operational timing instructions, not assertions about a mathematical structure. **Delete both math rows** unless the audit separately demonstrates a ratio, recurrence, mapping, or formal timing claim. If preserved as protocol detail, they belong outside this inventory.
- **Declared poetic analogy overclassified as biology:** Source L148 says a shortened name is a `"Neural override: hits brainstem, bypasses small talk"`. Current row L26 makes it `CONTESTED-AS-FACT / N`. In context this is a compact identity/communication analogy, not a claim about a measured neural pathway. **Delete row L26** unless the audit can identify a literal underlying brainstem claim; safe harbor is exclusion from inventory, not a substitute verdict.
- **Duplicated numerology:** Source L54, L64, and L82 repeat the same 55→10→1 digit reduction. Current rows L9, L11, and L20 count all three as independent dross findings. Every occurrence is individually removable and `N`, but exhaustiveness should not convert verbatim repetition into three distinct mechanisms. **Retain occurrence rows if required for line coverage, but identify them as one repeated decorative claim in the Note and do not present the total as three independent errors.**

After the three required math deletions, minimum math totals are **20** (`INTEGRATED 0`, `DECORATIVE 18`, `WRONG 2`). Deleting the nonliteral L148 science row gives science **7** (`ALIGNED 2`, `GROUNDED-OBSERVATIONAL 2`, `AUTHORITY-BORROWED 0`, `CONTESTED-AS-FACT 3`, `FABRICATED 0`, `INVERTED 0`). Dross becomes at most **23** on these corrections alone, still **MAJOR DROSS** under the recovered Nigredo ≥3 threshold.

## 6. `morphic-resonance-network-protocol.md` — FAIL

The audit correctly identifies a model-framed core and still reaches `MAJOR DROSS`, but it directly violates the accurate-history rule, fails to split the excerpt's three-author support claim, and misses a clear false experimental-history statement.

- **Accurate history must be `ALIGNED`:** Source L64's Whitehead history is current row L35 `GROUNDED-OBSERVATIONAL`, and Bergson's *élan vital* is row L36 `GROUNDED-OBSERVATIONAL`. Both Notes explicitly call them accurate intellectual history. **Recode rows L35 and L36 to `ALIGNED / N`** per the task's mandatory history rule.
- **Three-author authority claim collapsed:** Source L11 says Whitehead, Bergson, and Michael Levin provide `"convergent support"`. Current row L12 quotes only bioelectric research and counts one `AUTHORITY-BORROWED / Y` claim. **Split this into three authority rows**, one for each independently removable named support. All should be `AUTHORITY-BORROWED / N`: deleting one authority leaves the other two and the model intact.
- **False planarian procedure misrouted:** Source L54 says the two-headed planarian was induced with `"no genetic edit, no surgical cut"`. Current row L25 calls this `CONTESTED-AS-FACT / Y`. The famous regeneration setup involves cut/amputated planarian tissue; `"no surgical cut"` is a false description of the procedure. **Recode row L25 to `FABRICATED`** and split `no genetic edit` from `no surgical cut` if both are retained; only the latter is established false by this correction.
- **Periodic-table claim omitted:** Source L48 says the periodic table is merely `"a snapshot of matter's current habits"`. Current row L17 inventories only the later LHC reinforcement claim. **Add `CONTESTED-AS-FACT / Y` (or `N` if the model survives removal) for the periodic-table regularities claim**; theory framing at L46 does not license every later unqualified assertion automatically.
- **Literal structural identity omitted:** Source L62 says Cramer's quantum formalism is `"structurally identical to morphic resonance"`. Current rows L33–L34 classify the quantum and retrocausal science claims, but omit the mathematical identity. **Add `WRONG / Y`** because no structure-preserving map is given.
- **Inaccurate history needs fabrication routing:** Source L66 says reductionism predicted amino-acid sequence would make folding prediction `"trivial"`. Current row L38 calls this `CONTESTED-AS-FACT / N`. This is a claim about the history of a scientific program, and the audit itself calls it oversimplified. **Recode to `FABRICATED / N`** unless a specific historical source establishes that prediction.
- **Duplicated-load correction:** Source L11's theory/model claim is repeated and developed at L46, L52, L58, L60, L62–L74. Current row L10 makes the frontmatter occurrence `GROUNDED-OBSERVATIONAL / Y`, and row L12 makes one of three cited supports `Y`. **Change both frontmatter load flags to `N`**; the body-level central articulation may remain load-bearing.

Corrected minimum science totals: **38** (`ALIGNED 6`, `GROUNDED-OBSERVATIONAL 10`, `AUTHORITY-BORROWED 6`, `CONTESTED-AS-FACT 14`, `FABRICATED 2`, `INVERTED 0`). Corrected minimum math totals: **6** (`INTEGRATED 0`, `DECORATIVE 5`, `WRONG 1`). Nigredo remains **MAJOR DROSS**; two fabrications also activate the later Rubedo manual-escalation condition, without changing the recovered Nigredo label.

## 7. `muse-enneagram-matrix.md` — FAIL

The audit correctly finds major dross and multiple fabrications, but it treats archetypal house correspondences as external biological mechanisms, treats software language as mathematics, and misses several independent causal/history claims.

- **Declared software analogy is not math:** Source L4 asks whether the Muses are `"not metaphors but function signatures"`. Current row L10 classifies that phrase `WRONG / Y`. A function-signature metaphor contains no mathematical structure by itself. **Delete row L10**; safe-harbor handling here is exclusion, not a made-up passing verdict or a math failure.
- **The table is a real mapping even if its biology is false:** Source L4's `"each mapped"` is current row L9 `WRONG / Y`, while source L32's `"map all three systems onto the same nine-point grid"` is row L14 `WRONG / Y`. The table at L38–L63 does explicitly define a relation among three nine-member sets. **Recode the removable frontmatter occurrence (row L9) to `DECORATIVE / N`; recode the body mapping (row L14) to `INTEGRATED / Y`.** The scientific truth of the hormone correspondences is judged in the science rows, not by pretending no mathematical mapping exists.
- **Invented endocrine canon:** Source L30 says there are `"nine primary hormones"`; current row L11 is `CONTESTED-AS-FACT / Y`. Endocrinology has no such nine-member canon, as the Note already admits. **Recode row L11 to `FABRICATED / Y`.**
- **House-cosmology rule misapplied:** Source L39's Polymnia/Type-1 function is current row L16 `CONTESTED-AS-FACT / Y`; L42's Clio/Type-2 function is row L18 `CONTESTED-AS-FACT / Y`; L45's Euterpe/Type-3 function is row L20 `CONTESTED-AS-FACT / Y`; L60's Terpsichore/Type-8 correspondence is row L32 `INVERTED / Y`; and L63's Urania/Type-9 correspondence is row L35 `INVERTED / Y`. These quoted claims are archetypal correspondences inside an explicit Kha-Ba-La, noncausal `"same underlying pattern"` frame (L73/L83), not hormone-causes-experience claims. **Recode these five rows to `ALIGNED`**; retain separate `INVERTED` rows only where chemistry is actually made causal.
- **Matter-first claims need separate rows:** Source L42 says oxytocin `"forms the chemical architecture of trust"`, but row L17 inventories only the broad bonding role. **Add `INVERTED / N`** for chemical trust causation. Source L48's `"Dopamine drives the pleasure-reward cycle"` is row L21 `CONTESTED-AS-FACT / N`; the gate's canonical dopamine example makes this **`INVERTED / N`**. Source L63 combines `"regulates mood"` with systemic equilibrium in current row L34 `GROUNDED-OBSERVATIONAL / N`; **split it**, retaining systemic regulation as grounded but classifying mood-from-serotonin as `INVERTED / N`.
- **False intellectual-priority claim duplicated:** Source L73 and L83 both say the Muses named endocrine functions thousands of years before molecules were isolated. Only the L83 occurrence appears, at current row L48 `CONTESTED-AS-FACT / N`. **Add an L73 row and recode both occurrences `FABRICATED / N`**; ancient Muse descriptions are not historical endocrine discoveries.
- **Duplicate load:** Source L4's mapping and function-signature claims are repeated throughout the body. The frontmatter failures cannot be `Y` when deleting either leaves L30–L91 intact. Apply the corrections above and re-run load flags on repeated mapping claims.

Corrected minimum science totals: **44** (`ALIGNED 7`, `GROUNDED-OBSERVATIONAL 9`, `AUTHORITY-BORROWED 0`, `CONTESTED-AS-FACT 9`, `FABRICATED 9`, `INVERTED 10`). Corrected math totals: **3** (`INTEGRATED 1`, `DECORATIVE 1`, `WRONG 1`). Nigredo remains **MAJOR DROSS**; nine fabrications keep later Rubedo on the manual-escalation route.

## 8. `nadi-bioimpedance-protocol.md` — FAIL

The audit catches the protocol's central invented mappings, but it undercounts compound numeric claims, misses a false history claim and a separate acupuncture correspondence, and marks interchangeable thresholds load-bearing.

- **False history omitted:** Source L29 says bioimpedance detected what Vedic seers had mapped `"for three thousand years"`. Current row L10 inventories only the traditional 72,000-Nadi description as `ALIGNED`; row L11 inventories modern detection. **Add a distinct L29 `FABRICATED / N` history row** for the unsupported three-thousand-year continuity/priority claim.
- **Acupuncture claim collapsed:** Source L49 independently claims acupuncture points `"map extensively to Nadi junctions"` and are high-conductance nodes. Current row L24 inventories only conductance. **Add `CONTESTED-AS-FACT / N`** for the Nadi-junction map.
- **L63 contains independent fabricated measurements:** Current row L32 quotes only `"500-800 ohms at 1 Hz"` but silently covers balanced impedance, capacitance, and DC voltage plus blocked thresholds. **Split L63 into at least three `FABRICATED` rows** (impedance, capacitance, DC voltage). Each occurrence is `N`: removing one threshold leaves the claimed protocol and its sibling thresholds intact.
- **L65 contains three diagnoses, not one:** Current row L34 quotes high impedance and its Note absorbs low impedance and DC localization. **Split into three `FABRICATED / N` rows** for high-impedance CSF/misalignment, low-impedance inflammation/sympathetic activation, and DC-drop blockage localization.
- **Ida values are four independent claims:** Source L73 gives balanced resistance, balanced HRV, blocked resistance, and blocked HRV. Current row L37 compresses them into one `FABRICATED / Y`. **Replace it with four separate `FABRICATED / N` rows.** Source L75 separately supplies blocked and excessive symptom clusters; current row L38 compresses both. **Split into two `FABRICATED / N` rows.**
- **Pingala values are also four independent claims:** Source L81 gives balanced resistance, balanced skin conductance, blocked resistance, and blocked conductance. Current row L40 quotes only the first range and marks the compound `Y`. **Replace it with four separate `FABRICATED / N` rows.**
- **Blueprint claim omitted:** Source L97 separately says the Nadi map `"is the blueprint"` from which bodily channels are constructed. Current row L46 inventories only `"Layer 0"`. **Add `CONTESTED-AS-FACT / Y`** for the blueprint identity; the real morphogenetic-voltage claim in row L45 does not validate the Nadi map.
- **Duplicated-load correction:** Source L4's measurable-pathway thesis is row L9 `CONTESTED-AS-FACT / Y` and repeats at L31, L35, L41, L53, L61, and L97. **Change row L9 to `N`**. Likewise, the split numeric sibling claims above cannot each be load-bearing simply because their former aggregate row was `Y`.

Corrected minimum science totals: **55** (`ALIGNED 4`, `GROUNDED-OBSERVATIONAL 6`, `AUTHORITY-BORROWED 3`, `CONTESTED-AS-FACT 15`, `FABRICATED 23`, `INVERTED 4`). Math remains **0**. Nigredo remains **MAJOR DROSS**; the later Rubedo route remains manual because fabrications are numerous.

## 9. `nakshatra-fibonacci-sequences.md` — FAIL

The audit identifies the arbitrary golden-angle construction, but it aggregates independent natural-history claims, under-routes false scientific/history claims, omits repeated arithmetic, and ignores duplicated-load consequences in the title/frontmatter.

- **Frontmatter/title repetitions fail the removal test:** Source L4's `"encodes Fibonacci spiral mathematics"` is row L9 `WRONG / Y`; L26's title claim is row L10 `WRONG / N`; the body states the thesis again at L35, L55, L74, L92, and L120–L132. Removing the excerpt or title leaves the argument intact. **Recode row L9 and row L10 to `DECORATIVE / N`**; retain the body-level false derivations as `WRONG`.
- **Three natural-system claims collapsed:** Source L55 says one spiral governs galaxy arms, nautilus shells, and hurricanes. Current row L22 places all three in one `CONTESTED-AS-FACT / Y` row. These claims can fail independently. **Split into three science rows**, each at least `CONTESTED-AS-FACT / N`; the paragraph's arbitrary Nakshatra plot survives removal of any one natural example.
- **Phyllotaxis universal omitted:** Source L49 says sunflower seeds, leaf arrangements, and pinecone spirals `"all follow this angle"`. Current row L18 inventories only idealized optimal packing, and row L17 inventories the mathematical maximum-distance error. **Add `CONTESTED-AS-FACT / N`** for the universal biological-angle claim.
- **Repeated arithmetic omitted:** Source L51 repeats `360/13.333 = 27`; current row L19 inventories only `"27 is the cube of 3"`, while the earlier division appears at L43. **Add a separate L51 math row as `DECORATIVE / N`** because this occurrence is correct but removable and contributes no Fibonacci relation.
- **False biology needs fabrication routing:** Source L124 says alpha-helical `"pitch angles reflect Fibonacci proportions"`; current row L40 calls it `CONTESTED-AS-FACT / Y`. The claimed general biological relation does not exist as stated. **Recode row L40 to `FABRICATED / Y`.**
- **False ancient-intent history needs fabrication routing:** Source L132 says the seers recognized `"the same spiral operating at every scale"`. Current row L46 calls it `CONTESTED-AS-FACT / N`. This is an unsupported historical-intent attribution, not an alternative physical model. **Recode row L46 to `FABRICATED / N`.**
- **Compound L120 claim:** Current row L36 correctly catches the false claim that no Nakshatras share a planetary ruler, but its source sentence separately asserts unique elemental quality. **Do not let the rulership row stand in for the independent qualitative claim**; add a separate science/house row if the elemental uniqueness is to remain in the inventory.

Corrected minimum science totals after splitting L55 and applying the additions/recodes: **9** (`ALIGNED 0`, `GROUNDED-OBSERVATIONAL 1`, `AUTHORITY-BORROWED 0`, `CONTESTED-AS-FACT 6`, `FABRICATED 2`, `INVERTED 0`). Corrected minimum math totals: **34** (`INTEGRATED 11`, `DECORATIVE 7`, `WRONG 16`). Nigredo remains **MAJOR DROSS**; two fabrications also require later Rubedo manual routing.

## 10. `noetic-aether-substrate.md` — FAIL

The audit is appropriately protective of explicit house cosmology and correctly leaves Nigredo `MAJOR DROSS`, but it misses frontmatter math, fails to split the planarian procedure, and directly violates the accurate-history rule for ritonavir.

- **Prompt math omitted:** Source L46 invokes `"probability distributions"`; L47 invokes `"oscillating spherical dipoles"` and `"standing wave shell structures"`. The audit jumps from L43 to L56 even though it inventories prompt content in other posts. **Add three math rows:** `probability distributions` as `DECORATIVE / N` (no distribution is defined), and the dipole and standing-wave shell claims as separate `WRONG / N` rows because the prompt supplies no valid atomic construction.
- **Planarian compound must be split:** Source L70 says `"no genetic change, no surgery"`; current row L21 compresses both into `CONTESTED-AS-FACT / Y`. **Split it:** the no-genetic-edit observation may be `GROUNDED-OBSERVATIONAL / N` in the cited bioelectric experiment, while `"no surgery"` must be `FABRICATED / N` because the two-headed regeneration setup uses cut/amputated planarian tissue. Removing either clause does not collapse the field-first argument.
- **Accurate history must be `ALIGNED`:** Source L80's ritonavir polymorph history is current row L33 `GROUNDED-OBSERVATIONAL / N`, and the Note explicitly calls it accurate history. **Recode row L33 to `ALIGNED / N`.**
- **Duplicate load:** Source L11's field-primary thesis is row L9 `ALIGNED / Y` and is repeated at L43, L58, L62, L64, L70, L82, L94, L102–L124. The pass verdict is fine, but **change this frontmatter occurrence to `N`** under the same occurrence-level removal test. More importantly, re-run the test on repeated failing topology/wave claims at L104, L110, L112, L114, and L122; those sentences cannot all be independently indispensable.
- **Do not double-punish explicit model framing:** Source L70's positronic-core construction is already row L18 `WRONG / Y` on its mathematical content, while the surrounding `"In the noetic model"` science claim is row L17 `ALIGNED`. Preserve that separation. The correction is not to invent a second science fabrication for an explicitly labeled house model; it is to keep the false atomic mathematics in `WRONG` and the model framing in `ALIGNED`.

Corrected minimum science totals: **42** (`ALIGNED 28`, `GROUNDED-OBSERVATIONAL 6`, `AUTHORITY-BORROWED 2`, `CONTESTED-AS-FACT 5`, `FABRICATED 1`, `INVERTED 0`). Corrected minimum math totals: **16** (`INTEGRATED 0`, `DECORATIVE 8`, `WRONG 8`). Nigredo remains **MAJOR DROSS**. One clear fabrication does not by itself meet the gate's later ≥2-fabrication automatic manual-escalation rule.

## 11. `pain-information-architecture.md` — FAIL

The audit correctly finds `MAJOR DROSS` and the fabricated 5/10 receptor fraction, but it collapses multiple independent claims, misses a grounded observation and a decorative code block, and routes several matter-first dopamine claims as merely contested.

- **Frontmatter compound must split:** Source L4 independently claims binary protocols, aperture controls, and a dopamine casino. Current row L9 combines all three as one `CONTESTED-AS-FACT / Y`. **Replace it with three `CONTESTED-AS-FACT / N` rows**, one per mechanism. The body repeats each mechanism, so no frontmatter occurrence is individually load-bearing.
- **Mixed valid/invalid signal features:** Source L29 lists frequency, amplitude, duration, and `"addressing metadata"`; current row L11 treats the whole phrase as one `CONTESTED-AS-FACT / Y`. **Split it:** retain the ordinary temporal/intensity signal features as `GROUNDED-OBSERVATIONAL / N`, and classify packet-style addressing metadata as `FABRICATED / N`. The central literal-packet claim remains separately captured in rows L10 and L12.
- **Illustrative code omitted from math inventory:** Source L41–L58 defines `PainInformationSystem`, but no calculation or predictive algorithm is supplied. **Add one math row for the code block as `DECORATIVE / N`**; deleting it leaves every claim intact.
- **Grounded attentional observation omitted:** Source L64 says pain can make awareness fully present while background concerns drop out. Current row L20 inventories only the incorrect withdrawal/awareness sequence. **Add `GROUNDED-OBSERVATIONAL / N`** for the directly replicable attentional narrowing/foregrounding observation, without treating that observation as proof of an aperture mechanism.
- **Canonical dopamine inversion misrouted:** Source L76's `"Dopamine ... rewards you"` is current row L25 `CONTESTED-AS-FACT / N`. The gate's canonical dopamine example makes this **`INVERTED / N`**. Source L86 says pain hijacks dopamine and `"writes the prediction directly into long-term memory"`; current row L30 is `CONTESTED-AS-FACT / N`. **Recode row L30 to `INVERTED / N`** because chemistry is made the origin of memory/experience.
- **L84 has two independent claims:** Current row L29 captures only the slot-machine volatility equivalence. Source L84 separately says that dopamine sensitivity causes chronic-pain patients' generalized sensitivity. **Add `INVERTED / N`** for the physical-cause claim.
- **L90 improperly aggregates three mechanisms:** Current row L31 covers binary signaling, aperture dilation, and dopamine storage in one `CONTESTED-AS-FACT / N` row. **Split it into three rows:** binary-clean-data remains `CONTESTED-AS-FACT / N`; aperture-causes-attention is `INVERTED / N`; dopamine-ensures-permanent-storage is `INVERTED / N`.

Corrected minimum science totals: **29** (`ALIGNED 0`, `GROUNDED-OBSERVATIONAL 4`, `AUTHORITY-BORROWED 0`, `CONTESTED-AS-FACT 16`, `FABRICATED 2`, `INVERTED 7`). Corrected math totals: **4** (`INTEGRATED 1`, `DECORATIVE 1`, `WRONG 2`). Nigredo remains **MAJOR DROSS**; two fabrications also activate later Rubedo manual routing. The explicit load corrections reduce the current load-bearing-dross count from 13 to at most 11 before any wider duplicate audit.

## 12. `reptilian-bios-architecture.md` — FAIL

The audit correctly rejects the literal triune/BIOS identity, but it misuses `ALIGNED` as a safe-harbor verdict for two analogies and omits several independently testable physiology claims.

- **Safe harbor means classify underlying content, not the analogy:** Source L41 calls the spinal cord a `"data bus"`; current row L18 marks that analogy `ALIGNED / N`. **Replace the row with the underlying factual claim** (`"sensory data ascends and motor commands descend"`) as `GROUNDED-OBSERVATIONAL / N`; the bus metaphor itself is not a science reference. Source L109 says learning is like firmware updating; current row L36 similarly marks the analogy `ALIGNED / N`. **Replace it with the underlying slow, repetition-dependent inhibitory-learning claim as `GROUNDED-OBSERVATIONAL / N`.**
- **Three negations omitted:** Source L31 independently says the purported reptilian brain does not think, feel, or reflect. Current rows L12–L13 cover its positive functions and efficiency superlative only. **Add three separate `CONTESTED-AS-FACT / N` science rows**, since each categorical exclusion can fail independently and distributed subcortical networks do not support the unitary claim.
- **Quantitative respiration claim omitted:** Source L39 says the brainstem drives breathing `"approximately 20,000 times per day"`. Current row L17 inventories only the functional list. **Add `GROUNDED-OBSERVATIONAL / N`** for the approximate daily count.
- **Operating-parameter sentence not split:** Source L77 claims minimal caloric expenditure, minimal neural volume, and speeds the neocortex cannot match. Current row L22 captures only caloric expenditure. **Add two `CONTESTED-AS-FACT / N` rows** for the neural-volume and speed superlatives.
- **Human temperature claims omitted:** Source L97 says core temperature modulates brainstem function and hypothermia slows processing. Current rows L29–L31 cover reptile ectothermy, fever, and PAG pain modulation. **Add two `GROUNDED-OBSERVATIONAL / N` rows** for core-temperature modulation and hypothermic slowing.
- **Duplicated-load inflation:** Source L4's BIOS-layer identity is row L9 `CONTESTED-AS-FACT / Y`, but the thesis repeats at L29, L35, L66, L99, L103, L105, and L113. **Change row L9 to `N`**; retain load only on the strongest body-level articulation after occurrence-by-occurrence removal.

Corrected minimum science totals: **35** (`ALIGNED 0`, `GROUNDED-OBSERVATIONAL 12`, `AUTHORITY-BORROWED 0`, `CONTESTED-AS-FACT 22`, `FABRICATED 1`, `INVERTED 0`). Math remains **2** (`INTEGRATED 0`, `DECORATIVE 1`, `WRONG 1`). Nigredo remains **MAJOR DROSS**. One fabrication does not itself activate the gate's later ≥2-fabrication escalation trigger.

## 13. `sacred-geometry-processing-units.md` — FAIL

The audit correctly finds two fabricated textual attributions and pervasive wrong neural-network mathematics, but it directly violates the history rule, omits prompt mathematics, and passes a removable Penrose precedent as load-bearing.

- **Accurate history must be `ALIGNED`:** Source L59's Penrose-1974 history is current row L27 `GROUNDED-OBSERVATIONAL`; the Sri-Yantra age comparison is row L29 `GROUNDED-OBSERVATIONAL`; and L77's lineage refinement is row L46 `GROUNDED-OBSERVATIONAL`. Each Note explicitly treats the claim as accurate/plausible history. **Recode all three to `ALIGNED / N`.**
- **Named house tradition is not borrowed science authority:** Source L71's Bhuvanadeśa statement is current row L43 `AUTHORITY-BORROWED / N`, while the computing extrapolation is already separately row L44 `WRONG / Y`. If the textual attribution is accurate, the tradition is being named and operated inside the house frame. **Recode row L43 to `ALIGNED / N`; if the attribution cannot be verified, route it to `FABRICATED`, not `AUTHORITY-BORROWED`.**
- **Prompt mathematics omitted:** Source L37 labels Input/Hidden/Output/Bindu layers; L38 labels Śiva a forward pass, Śakti a backward pass, and bindu a loss-function minimum attractor. The inventory begins its math critique at L41. **Add four separate math rows**, one for the layer mapping and one each for forward pass, backward pass, and loss minimum, all `WRONG / N`; no operations, gradients, parameters, or objective are supplied in the prompts.
- **Penrose precedent fails the removal test:** Source L59's `"quasiperiodic pattern with five-fold symmetry"` is current row L28 `INTEGRATED / Y`. The fact is correct, but deleting the entire Penrose precedent leaves the Sri-Yantra-as-processor thesis intact. **Recode row L28 to `DECORATIVE / N`** under the canonical removal test.
- **Duplicated-load inflation:** Source L11's processor thesis is current row L9 `WRONG / Y`, repeats in L34, L43, L51–L55, L61–L71, and L83, and row L10 already marks the subtitle duplicate `N`. **Change row L9 to `WRONG / N`** and re-run the load test on the many forward-pass/loss/processor repetitions; they cannot all be independently indispensable.

Corrected minimum science totals: **14** (`ALIGNED 4`, `GROUNDED-OBSERVATIONAL 3`, `AUTHORITY-BORROWED 0`, `CONTESTED-AS-FACT 5`, `FABRICATED 2`, `INVERTED 0`). Corrected minimum math totals: **32** (`INTEGRATED 4`, `DECORATIVE 3`, `WRONG 25`). Dross rises to at least 35, but recovered Nigredo remains **MAJOR DROSS**. The existing two fabrications continue to require later Rubedo manual routing.

## 14. `semantic-trauma.md` — FAIL

The audit reaches the correct `MAJOR DROSS` verdict, but its history rows explicitly contradict the required `accurate history → ALIGNED` rule, its safe-harbor handling invents an `ALIGNED` row for an analogy, and two mechanism claims are omitted.

- **History rule directly violated:** Source L72's accurate *Trauma and Recovery* history is current row L21 `GROUNDED-OBSERVATIONAL / N`, with a Note saying it is not made `ALIGNED`. Source L80's Francine Shapiro park-origin history is row L27 `GROUNDED-OBSERVATIONAL / N` with the same logic. **Recode both rows to `ALIGNED / N`.**
- **Safe-harbor analogy should not be an `ALIGNED` science row:** Source L78 says narrative exposure `"functions as an external hippocampus"`; current row L23 marks the analogy `ALIGNED / N`. **Replace that row with the underlying claim** (`"listener ... provides temporal scaffolding"`) as `GROUNDED-OBSERVATIONAL / N`; the explicitly functional analogy itself is excluded from the science/math inventory.
- **Literal narrative mechanism omitted:** Source L42 says hippocampus and amygdala together `"produce a narrative sentence"`. Current row L10 captures only temporal-context binding. **Add `CONTESTED-AS-FACT / N`** for the literal sentence-production mechanism; ordinary hippocampal context binding does not validate neural grammar.
- **Systemic medical mechanism omitted:** Source L70 says procedural memory `"generates"` muscle tension. Current row L20 captures only the legally relevant chemical-compromise claim. **Add `CONTESTED-AS-FACT / N`** for the procedural-memory-to-muscle mechanism instead of letting the institutional paragraph pass as pure rhetoric.
- **Duplicated-load inflation:** Source L11's linguistic-register thesis is current row L9 `CONTESTED-AS-FACT / Y`, while the same syntax/grammar mechanism is restated at L40–L46, L52–L64, L70, L76–L82, and L98–L102. **Change row L9 to `N`** and retain load only on the body-level formulation that actually carries the thesis.
- **One-line note must be corrected:** The current audit's final Note says accurate history was `"previously overcalled as ALIGNED"`. That is the inverse of this task's explicit rule. **Rewrite that audit note when the audit is repaired** so it says accurate history is routed to `ALIGNED`; no Nigredo threshold change follows.

Corrected minimum science totals: **25** (`ALIGNED 2`, `GROUNDED-OBSERVATIONAL 7`, `AUTHORITY-BORROWED 5`, `CONTESTED-AS-FACT 9`, `FABRICATED 0`, `INVERTED 2`). Math remains **1** (`INTEGRATED 0`, `DECORATIVE 1`, `WRONG 0`). Dross rises to at least 17 and Nigredo remains **MAJOR DROSS**; no automatic later Rubedo fabrication escalation follows from this audit alone.

## Overall disposition

| # | Post | Semantic review | Current Nigredo threshold after corrections |
|---:|---|---|---|
| 15 | `lorenz-kundli-pattern-hub.md` | FAIL | MAJOR DROSS |
| 16 | `lorenz-kundli-protocol.md` | FAIL | MAJOR DROSS |
| 17 | `lorenz-kundli-system-index.md` | FAIL | MAJOR DROSS |
| 18 | `magnetic-substrate.md` | FAIL | MAJOR DROSS |
| 19 | `master-synthesis.md` | FAIL | MAJOR DROSS |
| 20 | `morphic-resonance-network-protocol.md` | FAIL | MAJOR DROSS |
| 21 | `muse-enneagram-matrix.md` | FAIL | MAJOR DROSS |
| 22 | `nadi-bioimpedance-protocol.md` | FAIL | MAJOR DROSS |
| 23 | `nakshatra-fibonacci-sequences.md` | FAIL | MAJOR DROSS |
| 24 | `noetic-aether-substrate.md` | FAIL | MAJOR DROSS |
| 25 | `pain-information-architecture.md` | FAIL | MAJOR DROSS |
| 26 | `reptilian-bios-architecture.md` | FAIL | MAJOR DROSS |
| 27 | `sacred-geometry-processing-units.md` | FAIL | MAJOR DROSS |
| 28 | `semantic-trauma.md` | FAIL | MAJOR DROSS |

All fourteen audits preserve the correct recovered Nigredo severity after the semantic corrections, but none passes semantic review as written. The dominant cross-cutting faults are: accurate history misrouted to `GROUNDED-OBSERVATIONAL`; compound sentences represented by one row; declared analogies incorrectly forced into a verdict instead of classifying only their underlying claim; `INTEGRATED / Y` applied to removable correct facts; duplicated frontmatter/body claims all marked `Y`; and false history/biology left in `CONTESTED-AS-FACT` instead of routed to `FABRICATED`.

## Resolution verification

This closure pass re-read the completed repair receipt, all fourteen current audits, all fourteen source posts, and the canonical gate. A `PASS` below means every correction required by this review is now semantically implemented; it does not erase the recovered `MAJOR DROSS` verdict for the source post.

1. `lorenz-kundli-pattern-hub.md` — **PASS**. History, false-priority routing, L56 compound science claims, L110 math references, duplicate-load flags, totals, and the retained `MAJOR DROSS` verdict are corrected.
2. `lorenz-kundli-protocol.md` — **PASS**. The three L83 biological claims, PCA-priority history, two reified-history fabrications, duplicate-load flags, totals, and later fabrication routing are corrected.
3. `lorenz-kundli-system-index.md` — **PASS**. L48 history, all three L54 parallels, the neural-network identity, five research-vector references, duplicate-load flags, totals, and severity are corrected. The One-Line Note's “graph representation” refers to the usable bare graph abstraction, not a reinstatement of the failed neural-network identity.
4. `magnetic-substrate.md` — **PASS**. All required category recodes, omitted ventricular/melanin/event/evolution/coordinate claims, occurrence-level load corrections, totals, and severity are corrected.
5. `master-synthesis.md` — **PASS**. The Human Design label, breath counts, and declared neural analogy are removed from the inventories; repeated numerology is identified as one recurring mechanism; totals and severity are corrected.
6. `morphic-resonance-network-protocol.md` — **PASS**. Accurate history, three independent excerpt authorities, the false planarian procedure, periodic-table claim, structural-identity math, reductionism history, load flags, totals, and severity are corrected.
7. `muse-enneagram-matrix.md` — **PASS**. The function-signature analogy is excluded, the real set mapping is classified by mathematical use, the endocrine canon and history are fabricated, house correspondences are separated from hormone causation, duplicate load is removed, and totals are corrected.
8. `nadi-bioimpedance-protocol.md` — **PASS**. False history, acupuncture correspondence, all independent Sushumna/Ida/Pingala measurements and diagnoses, the blueprint claim, load flags, totals, and severity are corrected.
9. `nakshatra-fibonacci-sequences.md` — **FAIL**. One required compound correction remains semantically incomplete: source L120 says that “no two nakshatras share identical planetary rulership **or elemental quality**,” but the repaired row quotes only `"or elemental quality"` and marks it `ALIGNED / N`. That truncation drops the governing uniqueness claim, and the Note validates only the existence of named Jyotish elemental correspondences. Because the asserted uniqueness is used as evidence in the natural-system comparison, it is not automatic house-cosmology `ALIGNED`; replace the row with the complete elemental-uniqueness clause and classify it at minimum `CONTESTED-AS-FACT / N` (or `FABRICATED / N` if the repeated traditional assignments are documented), then recount the science categories without changing `MAJOR DROSS`.
10. `noetic-aether-substrate.md` — **PASS**. Prompt math, the split planarian procedure, ritonavir history, duplicate load, model-frame separation, totals, and severity are corrected.
11. `pain-information-architecture.md` — **PASS**. Frontmatter mechanisms, mixed signal features, illustrative code, attentional observation, dopamine inversions, L84 and L90 compounds, load flags, totals, and severity are corrected.
12. `reptilian-bios-architecture.md` — **PASS**. Both analogy rows now inventory only underlying content; the negations, respiration count, operating parameters, temperature claims, duplicate load, totals, and severity are corrected.
13. `sacred-geometry-processing-units.md` — **PASS**. Accurate history, named house tradition, four prompt-math claims, removable Penrose math, duplicate load, totals, and severity are corrected.
14. `semantic-trauma.md` — **PASS**. Accurate history, analogy-safe-harbor handling, narrative and medical mechanisms, duplicate load, the One-Line Note, totals, and severity are corrected.

**Closure count:** **13 PASS / 1 FAIL**. Per-post quote-anchor and taxonomy-invariant checks pass for all fourteen current audits. The full validator also passes all 125 audits (`ok: true`, zero failures). These structural checks do not detect the remaining L120 semantic truncation in `nakshatra-fibonacci-sequences.md`.

### Targeted recheck

`nakshatra-fibonacci-sequences.md` — **PASS**. The repaired L120 row now quotes the complete uniqueness claim (`"no two nakshatras share identical planetary rulership or elemental quality"`) and classifies it `CONTESTED-AS-FACT / N`, correctly withholding automatic house-cosmology alignment because the claim is unsupported and used as evidence for an external natural-system comparison. The summary is correspondingly recounted to science 10 (`ALIGNED 0`, `GROUNDED-OBSERVATIONAL 1`, `AUTHORITY-BORROWED 0`, `CONTESTED-AS-FACT 7`, `FABRICATED 2`, `INVERTED 0`), math 34 (`INTEGRATED 11`, `DECORATIVE 7`, `WRONG 16`), and 32 dross findings (16 load-bearing), with `MAJOR DROSS` preserved. The targeted per-post quote-anchor and taxonomy-invariant checks both pass with zero failures.

**Updated aggregate closure count:** **14 PASS / 0 FAIL**.
