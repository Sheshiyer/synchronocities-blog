# Nigredo semantic repair receipt — manifest posts 15–28

**Repair scope:** Only the fourteen audits reviewed in `nigredo-semantic-review-15-28.md`
**Canonical gate:** Fool's Wisdom Grounding Gate v2.2.0
**Threshold rule:** Recovered Nigredo labels remain CLEAN / MINOR DROSS / MAJOR DROSS. Later Rubedo routing is recorded separately: any audit with ≥2 `FABRICATED` findings requires manual routing.

## 1. `lorenz-kundli-pattern-hub.md`

- History rule → L32 Lorenz publication, L32 Gleick, L34 *Surya Siddhanta*, and L66 *Brihat Parashara Hora Shastra* are now `ALIGNED / N`, each with an accurate-history Note.
- Fabrication routing → L34 `"Three thousand years earlier"` is now `FABRICATED / Y`; the Note identifies the unsupported identical-problem priority claim.
- L110 compound math omission → added separate `"golden ratio"` and `"recursive sequence frameworks"` rows, both `DECORATIVE / N`.
- L56 compound Lorenz claim → added separate nonrepeating-trajectory, deterministic, and unpredictable science rows, all `GROUNDED-OBSERVATIONAL / N`.
- Duplicated load → L4 and repeated exact-parallel/isomorphism occurrences at L60, L68, L74, L76, L88, L94, L96, and L129 are `N`; their Notes explain the surviving duplicate articulation.
- Recount → science 9 (4/4/0/0/1/0), math 35 (7/10/18), dross 29 (4 load-bearing), `MAJOR DROSS`. One fabrication does not itself activate later manual routing.
- Passed → `check-nigredo-quote-anchors.mjs --post lorenz-kundli-pattern-hub.md`; `check-nigredo-taxonomy-invariants.mjs --post lorenz-kundli-pattern-hub.md`.

## 2. `lorenz-kundli-protocol.md`

- L83 biological compound → endocrine shift, sleep fragmentation, and digestive change are three `CONTESTED-AS-FACT / N` rows.
- L71 false priority → added `"centuries before Pearson formalized the mathematics"` as `FABRICATED / N`, separate from the existing `WRONG` PCA identity.
- L93 model/history reification → both the billion-year Lorenz history and first-Venus continuity claim are `FABRICATED / N`.
- Duplicated load → frontmatter/subtitle `"same diagram in different notation"` rows at L11 and L38 are `WRONG / N`; the L45 body identity remains load-bearing.
- L83 load → the existing endocrine row changed from `Y` to `N`; both added siblings are also `N`.
- Recount → science 10 (2/1/0/4/3/0), math 45 (8/10/27), dross 44 (17 load-bearing), `MAJOR DROSS`. Three fabrications require later manual routing.
- Passed → `check-nigredo-quote-anchors.mjs --post lorenz-kundli-protocol.md`; `check-nigredo-taxonomy-invariants.mjs --post lorenz-kundli-protocol.md`.

## 3. `lorenz-kundli-system-index.md`

- History fabrication → L48 operational-for-millennia priority is `FABRICATED / N`; deleting the index introduction does not collapse the program map.
- L54 three-way compound → retained phase-space mapping and added scale-coherence and awareness-state-geometry rows; all three are `WRONG / N` after occurrence-level duplicate testing.
- Neural-network identity → L110 is `WRONG / Y`, because the graph lacks layers, activations, propagation, and trainable weights.
- Research vectors → added five independent `DECORATIVE / N` rows: Dasha prediction, coupled Lorenz oscillators, Lyapunov exponents, fractal dimension, and attractor reconstruction.
- Duplicated load → L4 topology, L54 state-space, L56 literal precision, and both L60 cross-application occurrences are `N`.
- Recount → science 2 (0/0/0/1/1/0), math 27 (5/6/16), dross 24 (8 load-bearing), `MAJOR DROSS`. One fabrication does not itself activate later manual routing.
- Passed → `check-nigredo-quote-anchors.mjs --post lorenz-kundli-system-index.md`; `check-nigredo-taxonomy-invariants.mjs --post lorenz-kundli-system-index.md`.

## 4. `magnetic-substrate.md`

- False facts → L84 only-paramagnetic-gas and L120 heart-chamber sequence are now `FABRICATED / N`.
- Vagal anatomy → L124 entire-GI innervation is `FABRICATED / N`; the existing distal-endpoint row remains separate.
- Ventricular pathway → added L104 choroid-plexus conduit as `FABRICATED / N`.
- Melanin compound → added distinct L176 entire-spectrum absorption and electromagnetic-to-cellular-instruction rows, both `FABRICATED / N`.
- L192 event list → cardiovascular surge remains its own row and Godwit deaths, bee collapse, and fish die-offs are three added `FABRICATED / N` rows.
- L210 evolution timeline → added separate mammal-first-appearance and Siberian-field-collapse rows, both `FABRICATED / N`.
- L224 coordinate identity → added `"sits at the same coordinates"` as `FABRICATED / N`.
- Duplicated load → all four L4 failures and the five repeated L63 summary failures identified by the report are now `N`; the later body mechanisms carry the argument.
- Recount → science 159 (2/25/6/29/89/8), math 5 (0/4/1), dross 137 (74 load-bearing), `MAJOR DROSS`. Eighty-nine fabrications require later manual routing.
- Passed → `check-nigredo-quote-anchors.mjs --post magnetic-substrate.md`; `check-nigredo-taxonomy-invariants.mjs --post magnetic-substrate.md`.

## 5. `master-synthesis.md`

- Human Design label → deleted the L88 `Gate 23.4` math row; the label performs no arithmetic.
- Breath instructions → deleted L175 `7-1-3` and L176 `4:4:4` math rows as operational timing details rather than mathematical structures.
- Safe-harbor analogy → deleted the L148 neural-override science row; no literal brainstem claim remains in the inventory.
- Repeated numerology → retained the three occurrence rows for coverage, but their Notes now identify one repeated digit-reduction mechanism rather than three independent mechanisms.
- Recount → science 7 (2/2/0/3/0/0), math 20 (0/18/2), dross 23 (1 load-bearing), `MAJOR DROSS`. No fabrication-based later manual route arises here.
- Passed → `check-nigredo-quote-anchors.mjs --post master-synthesis.md`; `check-nigredo-taxonomy-invariants.mjs --post master-synthesis.md`.

## 6. `morphic-resonance-network-protocol.md`

- Accurate history → L64 Whitehead and Bergson rows are `ALIGNED / N`.
- Three-author excerpt → replaced one aggregate authority row with separate Whitehead, Bergson, and Michael Levin `AUTHORITY-BORROWED / N` rows.
- Planarian procedure → the retained `"no surgical cut"` occurrence is `FABRICATED / Y`; the compound quote no longer hides which clause is false.
- Periodic-table omission → added L48 as `CONTESTED-AS-FACT / N`; the theory survives removal.
- Structural identity omission → added L62 `"structurally identical to morphic resonance"` as `WRONG / Y`.
- Inaccurate history → L66 trivial-folding prediction is `FABRICATED / N`.
- Duplicated load → L11 theory-model row and all three frontmatter authority supports are `N`; body articulations carry the model.
- Recount → science 38 (6/10/6/14/2/0), math 6 (0/5/1), dross 28 (11 load-bearing), `MAJOR DROSS`. Two fabrications require later manual routing.
- Passed → `check-nigredo-quote-anchors.mjs --post morphic-resonance-network-protocol.md`; `check-nigredo-taxonomy-invariants.mjs --post morphic-resonance-network-protocol.md`.

## 7. `muse-enneagram-matrix.md`

- Software analogy → deleted the L4 function-signature math row.
- Real mapping → L4 excerpt mapping is `DECORATIVE / N`; L32 body table mapping is `INTEGRATED / Y`.
- Endocrine canon → L30 nine-primary-hormones is `FABRICATED / Y`.
- House correspondences → the Polymnia, Clio, Euterpe, Terpsichore, and Urania rows are `ALIGNED / Y`, distinct from literal chemistry-causes-experience rows.
- Matter-first splits → added L42 chemical architecture of trust as `INVERTED / N`; L48 dopamine is `INVERTED / N`; L63 is split into grounded systemic equilibrium and `INVERTED / N` mood causation.
- False priority → added the L73 occurrence and recoded the L83 occurrence; both are `FABRICATED / N`.
- Duplicated load → the removable L4 mapping is `N`, and deleting the function-signature row removes its inflated `Y`.
- Recount → science 44 (7/9/0/9/9/10), math 3 (1/1/1), dross 30 (20 load-bearing), `MAJOR DROSS`. Nine fabrications require later manual routing.
- Passed → `check-nigredo-quote-anchors.mjs --post muse-enneagram-matrix.md`; `check-nigredo-taxonomy-invariants.mjs --post muse-enneagram-matrix.md`.

## 8. `nadi-bioimpedance-protocol.md`

- False history → added L29 three-thousand-year detection continuity as `FABRICATED / N`.
- Acupuncture compound → added L49 Nadi-junction mapping as `CONTESTED-AS-FACT / N`, separate from conductance.
- L63 measurements → replaced one aggregate row with six independent balanced/blocked impedance, capacitance, and DC-voltage `FABRICATED / N` rows.
- L65 diagnoses → replaced one aggregate row with high-impedance, low-impedance, and DC-localization `FABRICATED / N` rows.
- Ida values/symptoms → replaced one aggregate value row with four `FABRICATED / N` thresholds and split blocked/excessive symptoms into two `FABRICATED / N` rows.
- Pingala values → replaced one aggregate row with four independent `FABRICATED / N` thresholds.
- Blueprint omission → added L97 blueprint identity as `CONTESTED-AS-FACT / Y`.
- Duplicated load → L4 excerpt and all split numeric siblings are `N`; no aggregate `Y` survives.
- Recount → science 58 (4/6/3/15/26/4), math 0, dross 48 (18 load-bearing), `MAJOR DROSS`. Twenty-six fabrications require later manual routing.
- Passed → `check-nigredo-quote-anchors.mjs --post nadi-bioimpedance-protocol.md`; `check-nigredo-taxonomy-invariants.mjs --post nadi-bioimpedance-protocol.md`.

## 9. `nakshatra-fibonacci-sequences.md`

- Frontmatter/title duplicates → L4 and L26 are `DECORATIVE / N`; body derivations remain `WRONG`.
- Natural-system compound → L55 is split into galaxy, nautilus, and hurricane `CONTESTED-AS-FACT / N` rows.
- Phyllotaxis universal → added L49 `"all follow this angle"` as `CONTESTED-AS-FACT / N`.
- Repeated arithmetic → added L51 `"360/13.333 = 27"` as `DECORATIVE / N`.
- False biology/history → L124 alpha-helix proportions are `FABRICATED / Y`; L132 ancient universal-spiral intent is `FABRICATED / N`.
- L120 compound → the complete `"no two nakshatras share identical planetary rulership or elemental quality"` claim is `CONTESTED-AS-FACT / N`, separate from the existing `WRONG` repeated-rulership math row.
- Recount → science 10 (0/1/0/7/2/0), math 34 (11/7/16), dross 32 (16 load-bearing), `MAJOR DROSS`. Two fabrications require later manual routing.
- Passed → `check-nigredo-quote-anchors.mjs --post nakshatra-fibonacci-sequences.md`; `check-nigredo-taxonomy-invariants.mjs --post nakshatra-fibonacci-sequences.md`.

## 10. `noetic-aether-substrate.md`

- Prompt math → added L46 probability distributions as `DECORATIVE / N`, plus L47 spherical dipoles and standing-wave shells as separate `WRONG / N` rows.
- Planarian compound → split L70 into `"no genetic change"` as `GROUNDED-OBSERVATIONAL / N` and `"no surgery"` as `FABRICATED / N`.
- Accurate history → L80 ritonavir polymorph history is `ALIGNED / N`.
- Duplicated load → L11 field-primary excerpt is `N`; repeated wrong topology rows at L104, L110, and L112 are `N` after occurrence-level removal.
- Model framing → preserved L70 noetic-model science as `ALIGNED` and kept the positronic-core construction only in the existing `WRONG` math row.
- Recount → science 42 (28/6/2/5/1/0), math 16 (0/8/8), dross 24 (5 load-bearing), `MAJOR DROSS`. One fabrication does not itself activate later manual routing.
- Passed → `check-nigredo-quote-anchors.mjs --post noetic-aether-substrate.md`; `check-nigredo-taxonomy-invariants.mjs --post noetic-aether-substrate.md`.

## 11. `pain-information-architecture.md`

- Frontmatter compound → replaced one aggregate row with binary-protocol, aperture-control, and dopamine-casino `CONTESTED-AS-FACT / N` rows.
- Mixed signal features → L29 ordinary frequency/amplitude/duration is `GROUNDED-OBSERVATIONAL / N`; packet-style addressing metadata is a separate `FABRICATED / N` row.
- Illustrative code → added L41–L58 `PainInformationSystem` as `DECORATIVE / N`; it supplies no calculation or prediction.
- Attentional observation → added L64 `"awareness becomes total"` as `GROUNDED-OBSERVATIONAL / N`, separate from the disputed sequence.
- Dopamine inversions → L76 reward and L86 memory-write claims are `INVERTED / N`.
- L84 compound → added dopamine-sensitivity-causes-generalized-sensitivity as `INVERTED / N`, separate from slot-machine volatility.
- L90 aggregate → replaced it with binary-clean-data `CONTESTED-AS-FACT / N`, aperture-attention `INVERTED / N`, and dopamine-storage `INVERTED / N` rows.
- Recount → science 29 (0/4/0/16/2/7), math 4 (1/1/2), dross 28 (11 load-bearing), `MAJOR DROSS`. Two fabrications require later manual routing.
- Passed → `check-nigredo-quote-anchors.mjs --post pain-information-architecture.md`; `check-nigredo-taxonomy-invariants.mjs --post pain-information-architecture.md`.

## 12. `reptilian-bios-architecture.md`

- Safe-harbor underlying content → replaced L41 data-bus analogy with grounded bidirectional spinal pathways; replaced L109 firmware analogy with grounded slow repeated-exposure learning.
- Three negations → added separate L31 does-not-think, does-not-feel, and does-not-reflect `CONTESTED-AS-FACT / N` rows.
- Respiration count → added L39 approximate 20,000 daily breaths as `GROUNDED-OBSERVATIONAL / N`.
- Operating parameters → added L77 minimal-neural-volume and neocortex-speed superlatives as separate `CONTESTED-AS-FACT / N` rows.
- Human temperature → added L97 core-temperature modulation and hypothermic slowing as separate `GROUNDED-OBSERVATIONAL / N` rows.
- Duplicated load → L4 BIOS-layer excerpt changed from `Y` to `N`; the many body-level restatements preserve the thesis.
- Recount → science 35 (0/12/0/22/1/0), math 2 (0/1/1), dross 25 (8 load-bearing), `MAJOR DROSS`. One fabrication does not itself activate later manual routing.
- Passed → `check-nigredo-quote-anchors.mjs --post reptilian-bios-architecture.md`; `check-nigredo-taxonomy-invariants.mjs --post reptilian-bios-architecture.md`.

## 13. `sacred-geometry-processing-units.md`

- Accurate history → Penrose-1974, Sri-Yantra age comparison, and lineage refinement are `ALIGNED / N`.
- Named house tradition → L71 Bhuvanadeśa row is `ALIGNED / N`; its processor extrapolation remains separately `WRONG`.
- Prompt mathematics → added L37 layer mapping and L38 forward pass, backward pass, and loss-minimum attractor as four `WRONG / N` rows.
- Penrose removal test → L59 five-fold quasiperiodicity is `DECORATIVE / N`, not `INTEGRATED / N`; this canonical-gate adjustment preserves the invariant that removable correct math is decorative.
- Duplicated load → L11 processor excerpt and repeated L51 forward/backward, L53 loss, L61 attractor, L67 loss, and L83 operating-system occurrences are `N`; other body-level processor failures retain load.
- Recount → science 14 (4/3/0/5/2/0), math 32 (4/3/25), dross 35 (12 load-bearing), `MAJOR DROSS`. Two fabrications require later manual routing.
- Passed → `check-nigredo-quote-anchors.mjs --post sacred-geometry-processing-units.md`; `check-nigredo-taxonomy-invariants.mjs --post sacred-geometry-processing-units.md`.

## 14. `semantic-trauma.md`

- Accurate history → L72 *Trauma and Recovery* and L80 Shapiro park-origin rows are `ALIGNED / N`.
- Safe-harbor underlying content → replaced the external-hippocampus analogy with `"provides the temporal scaffolding"` as `GROUNDED-OBSERVATIONAL / N`.
- Narrative mechanism → added L42 literal narrative-sentence production as `CONTESTED-AS-FACT / N`.
- Medical mechanism → added L70 procedural-memory-generates-muscle-tension as `CONTESTED-AS-FACT / N`.
- Duplicated load → L11 linguistic-register excerpt changed from `Y` to `N`; body restatements preserve the thesis.
- One-line Note → now states that accurate history is routed to `ALIGNED`, correcting the prior inverse rule statement.
- Recount → science 25 (2/7/5/9/0/2), math 1 (0/1/0), dross 17 (11 load-bearing), `MAJOR DROSS`. No fabrication-based later manual route arises here.
- Passed → `check-nigredo-quote-anchors.mjs --post semantic-trauma.md`; `check-nigredo-taxonomy-invariants.mjs --post semantic-trauma.md`.

## Final verification

- Per-post quote-anchor and taxonomy-invariant commands passed for all fourteen repaired audits.
- Full 125-audit validation → `validate-nigredo-audits.mjs` returned `ok: true`, `audit_count: 125`, `parsed_count: 125`, `source_post_count: 125`, and zero failures.
- Global diagnostic context → `check-nigredo-quote-anchors.mjs --all --summary` still reports 71 pre-existing failures outside this 14-post slice; every owned `--post` invocation passes.
- Global diagnostic context → the 42-post taxonomy scan still reports three pre-existing failures outside this slice (`seventeen-ways-pattern-repeats.md`, `shadbala-tensor-field-theory.md`, `sri-yantra-geometry-that-doesnt-fit.md`); every owned `--post` invocation passes.
- Source status → `git status --short -- src/content/posts` and `git diff --name-only -- src/content/posts` both returned empty output; no source post changed.

## Targeted closure — `nakshatra-fibonacci-sequences.md` L120

- Resolution verification finding → replaced the truncated `"or elemental quality"` `ALIGNED / N` row with the complete ten-word L120 uniqueness claim.
- Canonical classification → `CONTESTED-AS-FACT / N`; the post supplies no direct evidence that all 27 elemental assignments are unique, and the claim supports an external natural-system comparison rather than merely naming house correspondences.
- Resulting summary → science 10 (ALIGNED 0, GROUNDED-OBSERVATIONAL 1, AUTHORITY-BORROWED 0, CONTESTED-AS-FACT 7, FABRICATED 2, INVERTED 0); math 34 (INTEGRATED 11, DECORATIVE 7, WRONG 16); dross 32 (16 load-bearing); `MAJOR DROSS` preserved.
- Targeted verification → per-post quote-anchor and taxonomy-invariant checks passed; full 125-audit validator passed with zero failures.
