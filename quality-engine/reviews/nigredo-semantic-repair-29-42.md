# Nigredo semantic repair receipt — manifest posts[28:42]

**Date:** 2026-07-18
**Canonical gate:** Fool's Wisdom Grounding Gate v2.2.0
**Scope:** fourteen owned Nigredo audits; source posts remained read-only
**Rule precedence:** The gate's §4.1/§4.4 removal test is authoritative. A mathematically correct but removable row is `DECORATIVE / N`; `INTEGRATED / N` is not a valid terminal state. Report bullets that requested `INTEGRATED / N` are mapped below to the gate-compliant result.

## 1. `seventeen-ways-pattern-repeats.md`

1. History routing: L64 “standard crystallography” and L113 Coxeter became `ALIGNED / N`; separate L113 Schattschneider and Conway/Burgiel/Goodman-Strauss rows were added as `ALIGNED / N`.
2. L66 was split into `INTEGRATED / Y` for the scoped periodic ornament and `WRONG / N` for the unqualified yantra/textile/Escher universal.
3. L125's correct eightfold restriction was added as `DECORATIVE / N`, not the report's `INTEGRATED / N`, because it repeats the load-bearing restriction theorem and survives removal.
4. L153 Kha/cadence was recoded to `WRONG / N`; house grammar does not supply translations or a group action.
5. Duplicate conclusion load was removed at L151 and L153; the cross-taxonomy L151 repetition was also set `N` to satisfy the strict removal test.

Result: science 18; math 31 (`INTEGRATED 11, DECORATIVE 2, WRONG 18`); dross 33; failing load 23; `MAJOR DROSS`. The one-row dross increase over the report minimum is the canonical `DECORATIVE / N` ruling above.

Passed: `node quality-engine/scripts/check-nigredo-quote-anchors.mjs --post seventeen-ways-pattern-repeats.md --summary`; `node quality-engine/scripts/check-nigredo-taxonomy-invariants.mjs --post seventeen-ways-pattern-repeats.md --summary`.

## 2. `shadbala-tensor-field-theory.md`

1. Added the six named bala definitions as separate `ALIGNED / N` rows plus the traditional six-dimension inventory row required by the corrected category minimum.
2. Added L59 independence as `WRONG / Y`; recoded L63's scalar/vector/matrix generalization to `WRONG / Y`.
3. Split L84 diagonal and off-diagonal placement into two `WRONG / N` rows.
4. Split L86 into contraction `WRONG / Y`, trace background `DECORATIVE / N`, and directional preservation `WRONG / N`.
5. Added the nonexistent L99 `calculate_tensor_field` construction as `WRONG / N`.
6. Split all four L110 gradient claims into separate wrong rows.
7. Added removable metric/electromagnetic background as decorative and false L120/L126/L128 applications as wrong.
8. Added L132 field coherence as `WRONG / Y` and L134 order/chaos as `DECORATIVE / N`.
9. Added both independent seer-history fabrications at L136, raising `FABRICATED` to six.
10. Added the Saturn, general physiology, and transit-body mechanisms as three contested rows.
11. Added L144's valid vector statement as `DECORATIVE / N`, not `INTEGRATED / N`, because the earlier vector representation makes it removable; deleted the L146 kernel/process analogy.
12. Set all three terminal L144 tensor repetitions to `N`.

Result: science 30; math 53 (`INTEGRATED 2, DECORATIVE 13, WRONG 38`); dross 70; failing load 27; `MAJOR DROSS`. Six fabrications preserve the separate later Rubedo `ESCALATE-TO-MANUAL` route; the Nigredo threshold remains unchanged.

Passed: `node quality-engine/scripts/check-nigredo-quote-anchors.mjs --post shadbala-tensor-field-theory.md --summary`; `node quality-engine/scripts/check-nigredo-taxonomy-invariants.mjs --post shadbala-tensor-field-theory.md --summary`.

## 3. `spolski-endocrine-framework.md`

1. Added the L33 biological-documentation thesis as `CONTESTED-AS-FACT / Y` and L37 convergence repeat as `N`.
2. Added Type 1, timing/wrath, and same-function rows for the Melatonin–Polymnia pairing.
3. Added the omitted oxytocin reinforcement, societal fragmentation, endorphin masking, cortisol scarcity, and Uranian over-identification mechanisms.
4. Split testosterone growth/development from competitive intensity, split serotonin mood from systemic balance, and added grounded L69 survival focus.
5. Enforced one failing `Y` per hormone–Muse pairing and made the L99 overall restatement `N`; exact failing load is 15.

Result: science 57; math 1; dross 51; failing load 15; `MAJOR DROSS`.

Passed: `node quality-engine/scripts/check-nigredo-quote-anchors.mjs --post spolski-endocrine-framework.md --summary`; `node quality-engine/scripts/check-nigredo-taxonomy-invariants.mjs --post spolski-endocrine-framework.md --summary`.

## 4. `sri-yantra-geometry-that-doesnt-fit.md`

1. Kulaichev was recoded to `ALIGNED / N`; Bühnemann and the 230-space-group history were added separately as aligned.
2. The 43-region, 44-total, eight-petal, and binomial rows are `DECORATIVE / N` under the removal test.
3. Added separate eight- and sixteen-petal rosette rows as `DECORATIVE / N`.
4. Added eighteen isometries as wrong; the duplicate correct ninefold restriction is `DECORATIVE / N`, not the report's impossible `INTEGRATED / N`.
5. Added all six omitted false planar/space-group deductions at L86/L88/L96 as `WRONG / N`.
6. Added L160's empirical-inhabitation repeat as `CONTESTED-AS-FACT / N`.
7. Set the L90/L138/L140/L142 D9-resolution repetitions to `N`.

Result: science 30; math 41 (`INTEGRATED 4, DECORATIVE 14, WRONG 23`); dross 58; failing load 13; `MAJOR DROSS`. Seven fabrications preserve later Rubedo manual routing; Nigredo remains on the recovered threshold.

Passed: `node quality-engine/scripts/check-nigredo-quote-anchors.mjs --post sri-yantra-geometry-that-doesnt-fit.md --summary`; `node quality-engine/scripts/check-nigredo-taxonomy-invariants.mjs --post sri-yantra-geometry-that-doesnt-fit.md --summary`.

## 5. `the-devil-in-the-detail.md`

1. Schultz experiment history became `ALIGNED / N`; the neural-result rows remain borrowed and the Cambridge mislocation remains fabricated.
2. Added RPE-as-substrate as `INVERTED / Y`.
3. Added separate grounded conditioned-stimulus and response rows.
4. Added the proprioceptive-field and identity-layer observations; split L72 intervention from “The loop broke.”
5. Recoded the pratyāhāra definition to `FABRICATED / N`.
6. Split urge surfing into accurate technique history `ALIGNED / N` and unsupported isolation mechanism `CONTESTED-AS-FACT / N`.
7. Added the L62 self-model mechanism as contested.
8. Recoded L90 nervous-system purpose to `INVERTED / N`.
9. Deleted the JSON software image from math inventory.
10. Deduplicated the paired L52 collapse mechanism and historical Schultz load.

Result: science 42; math 4; dross 31; failing load 11; `MAJOR DROSS`. Two fabrications preserve later Rubedo manual routing without altering Nigredo.

Passed: `node quality-engine/scripts/check-nigredo-quote-anchors.mjs --post the-devil-in-the-detail.md --summary`; `node quality-engine/scripts/check-nigredo-taxonomy-invariants.mjs --post the-devil-in-the-detail.md --summary`.

## 6. `the-downstream-mind.md`

1. Added separate aligned Schultz/macaque history rows at L282/L284.
2. Split L286 reward uncertainty into `CONTESTED-AS-FACT / N` while retaining the borrowed cue-prediction result.
3. Deleted the declared dopamine-aperture analogy; retained the later literal neurochemical aperture claim.
4. Recoded framework existence to `ALIGNED / N` and added separate contested ancient-taxonomy and Thalia–dopamine rows.
5. Deleted all three L392 lens-only math rows.
6. Added cortisol intensifying the field as `INVERTED / N`.
7. Recoded the explicitly framed L484 social-consciousness model to `GROUNDED-OBSERVATIONAL / N`.
8. Deleted L490 circle imagery from math inventory.
9. Set the removable L478 quine error to `N`.
10. Kept only the first 50/50 failure load and set repeated science/math rows plus the L504 authority stack to `N`.

Result: science 52; math 10; dross 48; failing load 17; `MAJOR DROSS`.

Passed: `node quality-engine/scripts/check-nigredo-quote-anchors.mjs --post the-downstream-mind.md --summary`; `node quality-engine/scripts/check-nigredo-taxonomy-invariants.mjs --post the-downstream-mind.md --summary`.

## 7. `the-ineffable-secrets-of-a-breathing-sprite.md`

1. Added five independent L51 vayu rows and the L55 applied-vayu observation as `ALIGNED / N`.
2. Deleted the L43 firmware and L79 pull-request software metaphors from science inventory.
3. Deleted the duplicate math row for quantum-vacuum physics vocabulary.
4. Set the frontmatter and every later repeated exclusivity/dual-innervation/atlas row to `N`, retaining the first body claims as load-bearing.

Result: science 44; math 4; dross 30; failing load 6; `MAJOR DROSS`. Nine fabrications preserve later Rubedo manual routing; Nigredo remains recovered-rule MAJOR.

Passed: `node quality-engine/scripts/check-nigredo-quote-anchors.mjs --post the-ineffable-secrets-of-a-breathing-sprite.md --summary`; `node quality-engine/scripts/check-nigredo-taxonomy-invariants.mjs --post the-ineffable-secrets-of-a-breathing-sprite.md --summary`.

## 8. `the-source-code-has-authors.md`

1. Tsarion and Montalk framework attributions were recoded to `ALIGNED / N`.
2. Deleted the conditional “If nature is habitual” row.
3. Deleted the L71 name-list authority row.
4. Deleted both inherited-code software metaphors from science inventory.
5. Added separate Whitehead “drops of experience” and process-of-becoming rows as aligned.
6. Added the Polymath Compass quadrant as `DECORATIVE / N`; deleted the mere micro-ritual count.
7. Kept only one load across precession, morphic-field, and birth-trauma duplicates.

Result: science 27; math 7; dross 21; failing load 7; `MAJOR DROSS`. Two fabrications preserve later Rubedo manual routing.

Passed: `node quality-engine/scripts/check-nigredo-quote-anchors.mjs --post the-source-code-has-authors.md --summary`; `node quality-engine/scripts/check-nigredo-taxonomy-invariants.mjs --post the-source-code-has-authors.md --summary`.

## 9. `the-sun-names-you.md`

1. Both Coulomb-barrier rows were recoded to `CONTESTED-AS-FACT / N`.
2. Added the Sun-as-combustion-chamber error as contested.
3. Added Vaiśeṣika sight history as `ALIGNED / N`.
4. Added the unchecked self-model, forced neural integration, prediction-machine, and threat-sequence rows with the report's contested/inverted taxonomy.
5. Added three independent house-cosmology rows for anandamaya and Kena Upaniṣad content.
6. Deleted the declared lens-curvature math row.
7. Added the earlier L80 Kha-Ba-La construction as `INTEGRATED / Y`; the later repeated L88 triad became `DECORATIVE / N`, not `INTEGRATED / N`, under the removal test.
8. Kept only one L44 literal-correspondence load and set L48/L92 repetitions to `N`.

Result: science 41; math 6 (`INTEGRATED 1, DECORATIVE 5`); dross 32; failing load 6; `MAJOR DROSS`. The one-row dross increase over the report minimum is the canonical repeated-triad ruling.

Passed: `node quality-engine/scripts/check-nigredo-quote-anchors.mjs --post the-sun-names-you.md --summary`; `node quality-engine/scripts/check-nigredo-taxonomy-invariants.mjs --post the-sun-names-you.md --summary`.

## 10. `the-tower-speaks-in-richter-scale.md`

1. Kept both fabrication inventory rows but changed frontmatter L11 to `N`, retaining body L25 as `Y`.
2. Changed the repeated L35 embodied observation to `N`.
3. Deleted the closing spiral metaphor from math inventory.

Result: science 8; math 3; dross 5; failing load 1; `MAJOR DROSS`. Two distinct fabrication occurrences preserve later Rubedo manual routing; any fabrication independently preserves Nigredo MAJOR.

Passed: `node quality-engine/scripts/check-nigredo-quote-anchors.mjs --post the-tower-speaks-in-richter-scale.md --summary`; `node quality-engine/scripts/check-nigredo-taxonomy-invariants.mjs --post the-tower-speaks-in-richter-scale.md --summary`.

## 11. `vimshottari-dasha-markov-chains.md`

1. Added the untraceable Pattern Recognition Protocol epigraph as `FABRICATED / N`.
2. Split L36 into contested Markov encoding and fabricated four-thousand-year history.
3. Recoded the early duration dictionary's “transition matrix” claim to `WRONG / N`.
4. Deleted microservice and scope/closure analogies from math inventory.
5. Added the L88–97 implementation contradiction as `WRONG / N`.
6. Added the rotated-sequence and exact-once trajectory errors as separate wrong rows.
7. Kept the frontmatter identity failure load and set L49/L110/L116 repetitions to `N`.

Result: science 12; math 32; dross 27; failing load 14; `MAJOR DROSS`. Four fabrications preserve later Rubedo manual routing.

Passed: `node quality-engine/scripts/check-nigredo-quote-anchors.mjs --post vimshottari-dasha-markov-chains.md --summary`; `node quality-engine/scripts/check-nigredo-taxonomy-invariants.mjs --post vimshottari-dasha-markov-chains.md --summary`.

## 12. `vortex-based-mathematics.md`

1. Split L43 into aligned Brahmagupta zero history plus two independent fabricated place-value/accounting claims.
2. Added the unsupported toroidal 3–6 leg as `WRONG / N`.
3. Split the number-line projection from the knot/dimensional-reduction row as another wrong claim.
4. Added the electromagnetic field-symmetry claim as `WRONG / N` alongside its science row.
5. Deleted the declared Tarot-lens pillar row from math inventory.
6. Kept only the first L83 universal-torus failure load and set the two paraphrases to `N`.

Result: science 15; math 33; dross 32; failing load 17; `MAJOR DROSS`. Two historical fabrications preserve later Rubedo manual routing.

Passed: `node quality-engine/scripts/check-nigredo-quote-anchors.mjs --post vortex-based-mathematics.md --summary`; `node quality-engine/scripts/check-nigredo-taxonomy-invariants.mjs --post vortex-based-mathematics.md --summary`.

## 13. `water-fourth-phase.md`

1. Pollack publication history became `ALIGNED / N`; its physical claims remain separately borrowed/contested.
2. Split all four missing frontmatter physical claims into separate `CONTESTED-AS-FACT / N` rows.
3. Added the hero's biological information-substrate mechanism as contested.
4. Split Levin lab history `ALIGNED / N` from the unsupported Ho/Pollack consequence `CONTESTED-AS-FACT / N`.
5. Added gel/charge-topology computation as contested and fascia-as-primary-signaling as a separate fabrication.
6. Deleted spiral, local-topology, and recursive-water metaphors from math inventory.
7. Set every listed repeated frontmatter/hero/body/material-first thesis row to `N`, retaining first full body statements as load-bearing.

Result: science 63; math 3; dross 58; failing load 6; `MAJOR DROSS`. Eight fabrications preserve later Rubedo manual routing.

Passed: `node quality-engine/scripts/check-nigredo-quote-anchors.mjs --post water-fourth-phase.md --summary`; `node quality-engine/scripts/check-nigredo-taxonomy-invariants.mjs --post water-fourth-phase.md --summary`.

## 14. `your-reality-is-a-smart-contract.md`

1. Recoded the Haṭha universal cellular-record attribution to `FABRICATED / N`; added accurate Arjuna/Kṛṣṇa textual application as `ALIGNED / N`.
2. Deployed immutable bytecode became `DECORATIVE / N`, not `INTEGRATED / N`: it is correct background but removable from the mantra thesis.
3. The four compiler children remain `DECORATIVE / N`, not report-requested `INTEGRATED / N`: each correspondence is demonstrated, but removing an individual child does not collapse the parent pipeline.
4. Deleted all nine listed standalone software-metaphor rows.
5. Recoded the deterministic trigger sequence to `WRONG / N`.
6. Kept only the first initiation-mechanism load at L48 and only the science load at L88.

Result: science 35; math 23 (`INTEGRATED 3, DECORATIVE 11, WRONG 9`); dross 43; failing load 10; `MAJOR DROSS`. The five-row dross increase over the report minimum is the canonical removal-test ruling for the five requested `INTEGRATED / N` rows. Four fabrications preserve later Rubedo manual routing.

Passed: `node quality-engine/scripts/check-nigredo-quote-anchors.mjs --post your-reality-is-a-smart-contract.md --summary`; `node quality-engine/scripts/check-nigredo-taxonomy-invariants.mjs --post your-reality-is-a-smart-contract.md --summary`.

## Slice verification

- All fourteen quote-anchor commands passed: exact normalized source substrings at exact line anchors, with every quote at or below fifteen words.
- All fourteen taxonomy-invariant commands passed after applying canonical `INTEGRATED / Y` versus `DECORATIVE / N` removal-test semantics.
- All fourteen summaries were recounted from final inventory rows; none merely copies a stated review minimum where canonical recoding changes category or dross totals.
- Every post remains `MAJOR DROSS` under the recovered Nigredo rule. Later Rubedo `ESCALATE-TO-MANUAL` is noted only where `FABRICATED >= 2`; it never replaces or modifies the Nigredo verdict schema.
- Full corpus gate passed: `node quality-engine/scripts/validate-nigredo-audits.mjs --expected-count 125 --complete` returned `ok: true`, `audit_count: 125`, `parsed_count: 125`, `source_post_count: 125`, and no failures.
- Source integrity passed: `git status --short --` across the fourteen owned `src/content/posts/*.md` files returned no output; no source post changed.
- Receipt hygiene passed: the receipt contains no unresolved-work placeholders.

## Targeted residual closure — resolution verification

The independent resolution pass identified five remaining canonical §4.1/§4.4 removal-test failures. These corrections supersede the interim totals in sections 1, 2, 11, 12, and 14 above; recovered Nigredo thresholds and separate later Rubedo routing remain unchanged.

1. `seventeen-ways-pattern-repeats.md`: recoded the removable correct rows at L59, L83, L111, L113, L133, and L151 from `INTEGRATED / Y` to `DECORATIVE / N`. Final science 18; math 31 (`INTEGRATED 5, DECORATIVE 8, WRONG 18`); dross 39; failing load 23; `MAJOR DROSS`.
2. `shadbala-tensor-field-theory.md`: recoded the duplicated L30 six-vector epigraph from `INTEGRATED / Y` to `DECORATIVE / N`. Final science 30; math 53 (`INTEGRATED 1, DECORATIVE 14, WRONG 38`); dross 71; failing load 27; `MAJOR DROSS`.
3. `vimshottari-dasha-markov-chains.md`: recoded the removable L116 formal-specification and L120 transition-matrix conclusions from `INTEGRATED / Y` to `DECORATIVE / N`. Final science 12; math 32 (`INTEGRATED 9, DECORATIVE 6, WRONG 17`); dross 29; failing load 14; `MAJOR DROSS`.
4. `vortex-based-mathematics.md`: recoded the duplicated L11 orbit, L47 power-of-two description, and L53 three-regime summary from `INTEGRATED / Y` to `DECORATIVE / N`. Final science 15; math 33 (`INTEGRATED 6, DECORATIVE 5, WRONG 22`); dross 35; failing load 17; `MAJOR DROSS`.
5. `your-reality-is-a-smart-contract.md`: recoded the removable frontmatter L11 smart-contract definition to `DECORATIVE / N` and added its body L40 occurrence as `INTEGRATED / Y`. Final science 35; math 24 (`INTEGRATED 3, DECORATIVE 12, WRONG 9`); dross 44; failing load 10; `MAJOR DROSS`.

Independent recount: science dross plus math `DECORATIVE` and `WRONG` rows yields exactly 39, 71, 29, 35, and 44 respectively; each failing load remains unchanged because the corrected true rows moved from load-bearing integrated usage to non-load-bearing decorative usage.

Verification:

- Quote-anchor checks passed for all five posts with zero failures: checked rows 49, 83, 44, 48, and 59 respectively.
- Taxonomy-invariant checks passed for all five posts with zero failures over the same row counts.
- `node quality-engine/scripts/validate-nigredo-audits.mjs --expected-count 125 --complete` returned `ok: true`, 125 audits parsed against 125 source posts, verdicts `CLEAN 23 / MAJOR DROSS 76 / MINOR DROSS 26`, and no failures.
